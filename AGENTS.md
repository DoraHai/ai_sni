# G-Snipers 项目协作规则(部署与安全红线)v3

> 本文件写给任何在这个项目上工作的 AI 编程工具或开发者。**开始改代码前必须先完整阅读。**
> 这是 2026-08-19 当天连续几次事故之后，用真实发生的证据写出来的，不是通用最佳实践，
> 是这个项目已经踩过的坑，而且铁律 0 描述的那次事故**已经在当天内被完整复现过一次**
> （见铁律 6），说明只读一遍不够，每次改动前都要重新对照检查清单。
>
> 建议放置位置：代码仓库根目录，文件名 `CLAUDE.md`（Claude Code 自动读取），
> 并另存一份为 `AGENTS.md`（多数 AI 编程工具正在采用的通用约定文件名）。
> 每个参与开发的编程窗口/AI 会话，在动手之前都应该先读这份文件。
>
> **当前开发集成基线：远程最新 `main`。** 日常 SEM 开发必须从最新 `main` 创建独立
> `codex/sem-<task-name>` 功能分支，审核通过后再通过 PR 合回 `main`。
> `codex/production-sem` 仅用于 SEM 前端生产发布；`codex/production-sem-backend` 仅用于
> SEM 后端生产发布基线。不得把任一生产分支当作日常开发起点，也不得用一个生产分支发布
> 另一端。这里的现行规则覆盖下方 2026-08-19 事故记录中的历史分支描述；完整流程见
> `HANDOVER.md` 2.1 节。

## 铁律 0：线上产物必须能追溯到唯一一个 Git 提交，任何时候都不例外（已部分修复，仍有缺口）

2026-08-19 排查发现：生产服务器 `/opt/sem-frontend/current` 指向的发布目录，**不是一次干净
构建的产物**，而是被多次手工替换单个文件"打补丁"拼出来的——目录里同时堆着 9 个不同日期
（从 8 月 17 号晚上到 8 月 19 号凌晨）的 `CampaignManageView-*.js`，其中一个文件名直接叫
`CampaignManageView-latest-merged.js`；还有一整套 `sem-morning-*` 前缀的重复文件、以及橙色
主题色 `#e86f1c`，这些内容一开始**在 Git 仓库的任何一次提交里都找不到**。

**后来找到了**：真实源码在本地机器 `D:\workspace\sem`，分支 `codex/sem-xlsx-report-export`
（提交 `0762769`，Carl 提交，已推送 GitHub），工作区干净、没有未提交改动。已经合并进
`codex/production-sem` 分支（见 `a255f4a`、`d26c723` 两次提交），并重新构建部署上线，
构建产物和线上逐字节一致，团队自己的 `npm run verify:sem-build` 校验通过。

**教训，即使问题已解决也要记住**：
- 这类"能构建出线上效果的真实源码只在某个人本地磁盘"的情况，只能靠**问人**解决，靠翻
  Git 历史翻不出来——这次搜遍了 71 个 GitHub 分支 + 30 个 PR + 5 个本地分支都没找到，
  换个思路直接问"最近谁在本地跑过这个、界面是什么样"，一句话就解决了。别在纯技术排查上
  死磕太久。
- **同一个功能被两个人/两条分支各自独立实现了一遍**（`codex/sem-xlsx-report-export` 上
  有一版简单的"设置投放地域"，`sem/campaign-region-management` 上又有一版更完整的批量版），
  彼此都不知道对方的存在。这是"改动不合并回主线、大家各自为战"的直接后果，不是巧合。

**"客户与模块""模块工作区"两个缺口后来解决了**：直接问 Carl 要到了源码（他一开始就没提交
过前端部分，Git 里翻不到很正常），已合并部署（`16384a7`）。**这一步印证了上面那条教训——
问人比继续翻 Git 快得多，早问 20 分钟就能省几个小时。**

**仍然没解决的缺口**：
- `frontend/src/views/seo/SeoSitesView.vue` 及其所属的整个"SEO 增长"侧边栏分组结构——
  源码本身在 `seo/intake-20260817`、`seo/release-consistency-20260818` 两个分支能找到，
  但它所属的导航/路由结构在当时 `codex/production-sem` 的源码基础上完全不存在，贸然单独
  接入这一个页面风险大于收益，没做。这个大概率也是"问一下最近做过 SEO 网站管理这块的人"
  比继续翻 Git 快。
- `auth-frontend`、`geo-frontend` 仍是冻结快照（`4937a6a`），真实源码还没找到，也没去问。

### 必须立刻做的事（不分工种，谁先看到这份文件谁先启动）

1. ~~把线上正在跑的这份代码原样拉回来，建立可追溯基线~~ ✅ 当时已完成，见 `codex/production-sem`
   分支的 `f2f03db`（后端）、`a255f4a`/`d26c723`/`16384a7`（前端）
2. ~~把已经写好但没合并的功能分支正式合并进来~~ ✅ 账户排期、地域定向（含批量操作）、
   推广账号、客户与模块、模块工作区五项已合并部署
3. `/seo/sites` 及其所属导航分组、`auth-frontend`、`geo-frontend` 三处仍待处理——按上面
   的教训，直接问人，不要再翻 Git
4. 合并完成、构建通过、`npm run verify:sem-build` 校验通过之后，**用
   `frontend/scripts/deploy-sem.sh` 这个脚本部署**，不要再手动 `scp`/手动改 `releases/`
   目录里的文件
5. 从这一刻起，`/opt/sem-frontend/releases/<时间戳>` 里的任何一个目录，都必须能用
   `git log` 查到对应是哪个提交构建出来的——做不到这一点，就说明流程又被绕过了

## 铁律 1：Git 是唯一权威版本来源

- 日常 SEM 功能改动（不管是人工改的还是 AI 会话改的）必须从远程最新 `main` 建独立功能
  分支，审核后通过 PR 合回 `main`。生产同步 PR 按下方前后端分支职责执行。不允许改完
  直接在本地发布，**更不允许直接改 `releases/<时间戳>/` 目录里的某个文件**（哪怕只改一个字）。
- 开始改动前，先确认本地分支已经同步到远程最新，避免基于一份过期代码继续开发。
- SEM 前端审核提交通过独立同步 PR 合入 `codex/production-sem` 后，才由现有前端 workflow
  自动发布；SEM 后端审核提交通过独立同步 PR 合入 `codex/production-sem-backend` 后，才允许
  从 `main` 手动运行 `Production SEM backend deployment`。
- 部署只能通过现有受控 workflow 和仓库发布脚本执行。前端发布脚本
  `frontend/scripts/deploy-sem.sh`
  自带"构建 → 本地校验 → 上传新版本目录 → 远端二次校验关键字符串 → 原子切换软链 → 切换后再
  确认一次"的完整链路；后端 workflow 只能调用 `platform-deploy apply sem`。不要绕开门禁手动
  操作，也不要自行发明快速发布命令。
- 普通 SEM 前端或后端发布必须记录 `migration=not-run`，不得执行 `alembic upgrade`、
  `downgrade` 或 `stamp`。确需 Schema 变更时，必须另行只读诊断、审核和明确授权。
- 如果你不确定线上当前是什么版本，先去问，不要凭本地文件夹内容假设。

## 铁律 2：模块边界，不跨界顺手改

参考 `HANDOVER.md` 已有的规定：

- SEM / SEO / GEO / 官网门户 / 登录页 各有各的负责范围和独立发布单元
- 即使同一个仓库里能看到其他模块的代码，也不要为了当前需求顺手改动、顺手发布其他模块
- 尤其是**共享的底层文件**（鉴权、models、writeback 编排逻辑）——改之前确认这次改动的
  必要性，改完要清楚知道自己动了哪些其他模块也在依赖的代码
- **加新功能时只改这个功能真正需要的文件。** 2026-08-19 案例：账户排期+地域定向这两个功能，
  实际只需要改 `frontend/src/api/manage.js` 和 `frontend/src/views/manage/CampaignManageView.vue`
  两个前端文件；但同一个分支里混进了 GEO 菜单整体移除、门户导航改版等大量不相关改动，导致
  没法干净地只取需要的部分，最后只能放弃合并那整个分支。**功能分支保持单一职责，方便别人挑着合并。**

## 铁律 3：这些安全机制不能被静默移除

以下是历史上被修复过、且理论上可能在某次"顺手改动"或"基于旧分支开发"时被覆盖掉的关键点。
**任何改动到下列文件前，先确认这些机制还在，改完也要确认还在：**

| 文件 | 必须保留的机制 | 怎么快速确认 |
|---|---|---|
| `app/baidu/writeback.py` | 每个 `apply_*_writeback` 函数读取要改的行时用 `.with_for_update()` 加锁 | `grep -n with_for_update app/baidu/writeback.py`，应该在 9 处以上出现 |
| `app/baidu/writeback.py` | 真实资金回写（非 dry-run）前调用 `_claim_funds_approval` / `claim_approval` | `grep -n claim_approval app/baidu/writeback.py` |
| `app/security/auth.py`、`app/security/api_key.py` | `?key=` 查询参数只有 `admin_api_key_query_enabled=True` 时才生效，默认必须是 `False` | `grep -n admin_api_key_query_enabled app/config.py`，默认值应为 `False` |
| `app/main.py` | 启动时调用 `enforce_production_secrets`（生产环境拒绝占位符密钥） | `grep -n enforce_production_secrets app/main.py` |
| `app/api/auth.py` | 登录失败次数锁定（`failed_login_attempts` / `locked_until`），并用行锁防止并发计数丢失 | `grep -n with_for_update app/api/auth.py` |
| `app/seo_crawler.py`、`app/seo_distribution.py` | 抓取/发布外部 URL 前校验目标是公网 IP（防内网 SSRF） | 搜索 `is_global` 相关校验函数 |
| `frontend/scripts/verify-sem-build.mjs` | 前端构建产物必须包含关键接口/文案标记，且不能含有过时占位文案 | `npm run verify:sem-build`，失败就不能部署 |

如果发现某一项检查不到，**不要假设"应该没问题"，先去确认是不是被覆盖了。**

## 铁律 4：真实资金操作前的额外确认

- `BAIDU_WRITE_DRY_RUN` 目前是 `true`（演练模式，不会真花客户的钱）。除非项目负责人明确
  说明要关闭，任何情况下都不允许修改这个开关或绕过它去测试真实写回。
- 一旦要关闭 dry-run，必须先完整走一遍铁律 3 表格里 `writeback.py` 的两条机制确认清单，并做
  一次并发场景的实测（比如同时提交两次同一关键词的调价请求），而不是只看代码没问题就当作没问题。

## 铁律 5：判断"功能做完了"要看完整度，不要看字符串搜得到就算数

2026-08-19 案例：排查"地域定向"功能是否已上线时，第一次搜到相关接口字符串就以为"没做"，
后来才发现基础版界面其实已经在线上，只是缺了批量操作这部分——**搜到关键词只能证明"有一部分
存在"，证明不了"功能完整"**。验收一个功能是否真正完成，请对照功能设计里列出的每一个具体交互
点（比如"支持单个操作"和"支持批量操作"是两件要分别确认的事），不要以偏概全。

## 铁律 6：合并/替换整个前端基础时，部署前必须做"导航完整度"对比，不能只看新功能加进去了没

2026-08-19 真实事故：把前端从"冻结快照"换成真实源码构建部署时，只确认了"要加的新功能
（账户排期、地域定向批量操作）有没有进去"，**没有反向确认"旧版本里已经在用的功能有没有
被换没了"**。结果部署后"客户与模块""模块工作区"这两个原本在导航菜单里、真实可点击使用的
页面直接消失了几分钟，直到主动去核对导航才发现，紧急回滚。

**这次事故说明：新旧两个前端版本要合并/替换时，"新功能清单"和"旧功能清单"必须分开各自
核对一遍，只核对前者会漏掉后者。** 具体做法（已经验证有效，之后照做）：

1. 部署前，从**当前线上**的构建产物里提取所有路由路径：
   `grep -oE 'path:\`/[a-zA-Z0-9/_-]+\`' <当前线上入口js> | sed 's/path:\`//;s/\`$//' | sort -u`
2. 从**即将部署的新构建**里提取路由源码里的路径：
   `grep -oE "path: ?['\"][^'\"]+['\"]" frontend/src/router/index.js | sed -E "s/path: ?['\"]//;s/['\"]\$//" | sort -u`
3. `comm -23` 两份列表，看"线上有、新版本没有"的部分——这里出现的每一项都必须能解释清楚
   "为什么没有"（比如确认是外链、确认是别的模块负责），解释不清楚就不能部署
4. 只有第 3 步过了，才去部署；部署后再实测点开几个关键菜单项，不能只看 curl 返回 200

## 发布前自检清单（建议加进部署脚本或 PR 模板）

- [ ] 功能分支从远程最新 `main` 创建，没有基于过期代码或生产分支开发
- [ ] 这次改动没有涉及不在计划范围内的其他模块目录
- [ ] 如果碰到了 `writeback.py` / `security/*` / `main.py`，跑过铁律 3 表格里对应的 grep 确认
- [ ] 前端改动跑过 `npm run verify:sem-build` 并且通过
- [ ] **如果是替换/合并整个前端基础（不是改单个页面），跑过铁律 6 的导航路径对比，
      确认没有丢失线上已有功能**
- [ ] SEM 发布提交已通过独立同步 PR 进入正确的前端或后端生产分支，没有跨端混用
- [ ] 部署通过现有受控 workflow 和仓库发布脚本完成，不是手动 scp/改文件
- [ ] 普通 SEM 发布记录 `migration=not-run`，且没有执行任何 Alembic 命令
- [ ] 如确需数据库迁移，已经完成独立只读诊断、人工审核和明确授权，未隐藏在应用发布中
- [ ] 发布后做一次基本健康检查（首页/登录页/关键 API 返回码），而不是发完就当作结束
