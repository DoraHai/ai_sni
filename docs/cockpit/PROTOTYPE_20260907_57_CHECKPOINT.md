# G-Snipers 获客工作台：原型57代码检查点

本提交保存本地原型的HTML、CSS、JavaScript、离线测试及本说明。全部为交互演示，不代表生产接入或完成正式验收。

## 启动

进入 cockpit/prototype 后使用Python3：

    python -m http.server 8898 --bind 127.0.0.1

打开 http://127.0.0.1:8898/cockpit-prototype.html 。必须通过HTTP加载ES modules，不能仅复制HTML或用file协议打开。

## 本批功能

- 对话和可下钻的数据看板；对象详情、方案确认、本地待办与模拟负责人处理。
- SEO 发布/页面检查/搜索表现各自表达；单篇点击未知，不分摊主题点击。
- GEO 缺失/零值/不可比场景、正式单位映射、跟进草稿结构化演练标记。
- 七种模块开通组合的主看板、对应任务、health说明与对话建议；默认显式三模块演示。历史对话按原组合折叠，未知行动类型不默认归SEM。

## 验证

50项HTML本地资源引用齐全，33项JS语法检查通过。panorama.test.cjs、demo-finish.test.cjs、module-scope.test.mjs通过。浏览器检查七组合卡片、SEO/GEO状态、方案保存/撤回、对话往返和旧组合折叠。SEO/GEO开发者对各自消费规则进行了只读复核，三项模块范围问题修复后复核通过。

## 仍需推进

- 单/双模块模式的旧独立功能导航目前回到主看板，尚未完成所有旧页面适配；不能宣称全功能模块隔离完成。
- 当前话术是演示逻辑，没有真实AI推理或API消费；展示开关不授予权限。
- 本地任务含独立演练，生产任务持久化、接口外发和完成度统计需另行接入，不直接复用浏览器记录。
- SEM/SEO普通账号HTTP验收仍待执行，不能将准备完成视为接口验收通过。
- 用户转回的管理员记录已确认：验收包上传至 `/tmp/workbench-readonly-20260907` 且校验通过；SEM版本为 `22fcb30a0509378f0e347559004ffb79fc937500`，SEO版本短SHA为 `5ef5bab99b5c`。以上为管理员回传，非本原型独立线上验收。
- 获准专用测试资源为租户16、只读用户5/角色4（约定六项view权限）、SEM账号14及SEO站点3；账号14和站点3保持disabled。不是原真人客户的site_id=1。资源准备已确认，实际请求中的身份、归属与权限仍由验收脚本核验。
- GEO五表前后计数补充核对已完成，不等于任意时间绝对无写入证明。
- 本提交不包含管理员凭证或服务器验收包，不涉及共享认证、数据库或生产部署。
