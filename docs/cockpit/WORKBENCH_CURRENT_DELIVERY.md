# G-Snipers 获客工作台：当前交付入口

更新：2026-09-07 04:39（上海时间）。本文件记录已验证的交付，不替代实时发布记录。

## 代码和查看入口

- 本地原型：http://127.0.0.1:8898/cockpit-prototype.html
- main 已合并的原型/消费层最新提交：94aba12eb8675ee8f04ae88d04e2e4bbc2c3a16c（PR #403）。
- 原型本地演示分支：codex/cockpit-foundation，检查点d951823。该分支还有早期后端骨架，不能整体当成生产发布包。
- 静态原型源码：cockpit/prototype/。服务器根目录应为该目录；可用python -m http.server 8898 --bind 127.0.0.1运行。
- 主入口资源分别使用版本号，SEM独立演示模块版本为20260907-63，不意味着所有旧资源都改成63。

## 已完成的本轮工作

1. 原型独立抽取并合入main，自动执行语法和原型回归（#396、#397）。
2. 七种模块组合的详情窗范围、导航、模块明细和全部功能搜索已适配（#398至#401）。旧模块页停用；单/双模块的旧经营分析仍未开放。
3. SEM纯展示函数保留缺报、零值、部分小计和覆盖未知（#402）；没有默认网络transport。
4. SEM独立演示支持缺日报、真实零值、电话点击部分依据三场景，可查看依据并在对话中往返（#403）。与既有SEO/GEO演示一样，不是客户实测数据。
5. 内容/转化演练的待办归属、跨组合隐藏及恢复已检查，无需另行修复。

主界面以客户与智能体对话为核心，数据图表可以下钻和辅助解释。发现与解释分开，主题相关不代表因果。

## 确认过的验证边界

每个上述PR分别经过代码审查、相关测试和CI后合并；#403九项CI通过。开发者浏览器验证与统筹独立回归分别记录，不能将前者称为统筹浏览器验收。

原型副本的SEM展示函数与integrations源文件逐字检查；源文件变更也触发CI。原型页面未接真实AI或生产API，待办只是本地演示记录，没有发出业务任务。

## 仍待完成

- SEM/SEO普通只读身份HTTP验收结果尚未回传。验收包已由管理员确认上传至服务器/tmp/workbench-readonly-20260907并通过校验，不再要求重新找脚本。
- 专用获准测试范围：tenant16、user5/role4、SEM account14、SEO site3；六项view权限，account14/site3保持disabled。凭证不写入本文。
- SEM测试关键词900000001/900000002，仅2026-09-05有合成报告。不能把该样例覆盖当成全部多账户验收已完成。
- GEO五个只读GET及前后五表计数已有回传；延迟两次计数不证明任意时间绝无写入，不等于完整GEO业务验收。
- 真实身份传递、生产数据消费、信号规则、AI对话和受控任务执行仍需按阶段独立实现/验收。
- SEM开发任务仍缺新进度回传；统筹新增SEM展示层不表示SEM窗口自行恢复开发。

## 发布与协作

本轮main合并没有触发生产发布。SEM/SEO/GEO各走自己的受控发布流程，不直接把工作台原型推到模块服务。数据库迁移与共享认证不在本轮范围。测试资源到期停用和归档，不自动删除审计记录。

三模块接口/数据口径以各自契约为准；历史COCKPIT_HANDOVER中的概念稿和阶段描述仅供背景，不能当成已部署或已接真实服务的证明。
