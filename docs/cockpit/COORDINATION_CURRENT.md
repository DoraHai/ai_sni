# Current coordination — 2026-09-08 20:58 Asia/Shanghai

This is the authoritative continuation checkpoint. Historical pause documents are superseded by the current user instruction to continue development and coordinate the existing SEM, SEO and GEO tasks.

- 2026-09-09 00:18 GEO customer-review Draft PR499 exact `d6e5d9743f1fbdb65e8e208065d6ecdb05ba187e` on production-geo `4bb78059549a4b2819bf0d525c98f2fdc2bcb726` is blocked at coordinator review P1=1. The exact review route and concurrency guards are present, but `is_tenant_customer_reviewer` grants review writes to every tenant-bound `geo.content:view` account, including custom/read-only workbench identities; the frontend mirrors the same overbroad rule. The owner must restrict the exception to the real system `品牌方客户` role plus bound tenant and view access, retain the backend as final authority, and prove other tenant-bound read-only roles remain 403/no controls. PR499 remains Draft and undeployed; no production role, database, task14, review or publication action ran.

- 2026-09-08 23:46 GEO H2 human acceptance is blocked by a confirmed review-permission defect, not by sequence or state. On production `63fefec53906c99d78c3d800395af44afd72d1e1`, 诺德 task 14 was correctly in `等待客户确认`, still V5/article 22 with unchanged channel drafts. `nord_geo_review` clicked customer approval once; `POST /api/v1/geo/content-tasks/14/review` returned HTTP 403 `当前角色无权编辑此功能`, and the page remained pending. No privilege expansion, publication account, publish or H3/H4 action occurred. The GEO owner must fix this in code from latest production-geo using least privilege: a bound customer reviewer may approve/reject only the current pending version for its tenant, while remaining unable to edit/generate/distribute/publish or cross tenants. Direct production role/DB changes are forbidden; any new permission key or migration must stop for coordinator/database review. A Draft PR with negative privilege regressions is required before another human retry.

- 2026-09-08 23:28 GEO H2 current blocker is an omitted workflow prerequisite, not yet a proven reviewer-permission defect. The first `nord_geo_review` attempt occurred while task 14 still displayed `待提交客户审核`; GEO requires an authorized editor to submit the current V5/article 22 into `pending` before the reviewer can approve/reject. The immediate human sequence is: complete the editor/superadmin CAPTCHA, verify 诺德 and unchanged V5/article 22, click `提交客户审核` exactly once, then sign out and use `nord_geo_review` to approve exactly once. If the task already shows pending and the reviewer still receives `当前角色无权编辑此功能`, stop and return the review endpoint HTTP status/error without widening permissions. No real publication, account configuration or H3/H4 action is allowed.

- 2026-09-08 22:39 GEO H1/H2 acceptance record PR498 exact `a1ee1437a1ab983bdb8ee77351dfcdce9f250514` passed coordinator review P1=0/P2=0 and squash-merged to production-geo as documentation-only commit `4bb78059549a4b2819bf0d525c98f2fdc2bcb726`. The commit used `[skip ci]`; two post-merge checks found zero Actions runs for that head, so no deployment was queued or cancelled. Production runtime remains code SHA `63fefec53906c99d78c3d800395af44afd72d1e1`. H2 still waits only for the human CAPTCHA/reviewer flow; no login, task 14 operation, migration, deployment or business call ran during the documentation merge.

- 2026-09-08 21:38 GEO H1 final human acceptance passed on deployed `63fefec53906c99d78c3d800395af44afd72d1e1` for 诺德 task 14. The tester added three extractable steps using existing facts, saved once, ran publication refresh and current-brand recheck once each, and obtained article 22 / master V5 (from V4), GEO score 94 and `可以发布`; the prior missing-step blocker is gone and V1/V2/V3 remain. No regeneration, approval, channel publication or real provider action ran. H2 Phase A also passed: website/WeChat/Zhihu channel drafts exist and pre-review push is disabled with no publication account. The superadmin session is closed and `nord_geo_review` credentials are filled; the sole immediate human step is entering the CAPTCHA and completing reviewer login, after which H2 must verify approval, a small-edit invalidation, old-approval publish rejection and channel-draft regeneration without any real publication. H3/H4 remain untouched.

- 2026-09-08 21:54 SEO disabled-action release completed. Reviewed PR497 exact `39417d7a85abd15f0cdc47d3d93f13d68435c4f7` merged to production-seo as `0867004478504ce6d5b848338af4b4dc420556f9`; deployment run `34234227135` succeeded. Active backend/frontend are the `/opt/.../20260908T134930Z-086700447850` pair; previous/rollback are the `/opt/.../20260908T113409Z-a5a5747120cb` pair; `migration=not-run`. Independent public verification confirmed `/seo-health` 200 with db/schema ok and revision `0094_seo_qa_batches`, `/seo/` 200, and current entry/session/CSS assets 200. Seven localhost 8020 connection refusals were limited to the expected service-switch window and recovered before workflow success; no persistent startup error or rollback occurred. No login, customer data, collection, generation, task execution, connection test or real platform publication ran.

- 2026-09-08 21:38 SEO disabled-action PR497 final candidate exact `39417d7a85abd15f0cdc47d3d93f13d68435c4f7` on unchanged production-seo base `a5a5747120cb0c783342ccf9b7918e53cf265490` passed coordinator final review P1=0/P2=0. It adds a fresh persisted publication→content→site entitlement read immediately before ordinary publish/retry and video upload/publish provider calls, closes durable attempts without calling the provider when revoked, and still records real provider outcomes after an external call has occurred. GitHub reports open/Draft/clean with all five checks green; independent focused verification passed 88 tests with 1 skip. The SEO owner is authorized to merge/deploy only this unchanged exact head to production-seo, with no migration, collection, generation, customer login or real platform publication, and must return active/previous release, health/assets/log and no-business-call evidence.

- 2026-09-08 21:17 SEO disabled-action Draft PR497 second head `5db3178805540d8ed54457ae95519dd846d6a375` fixed the first resource-ID/site-scope P1 and has green CI plus broad local coverage, but coordinator re-review found a remaining P1 in irreversible provider actions. Ordinary distribution publish and video upload/publish persist their reservation/attempt and then call the provider without a fresh operational-site/entitlement read; an administrator revocation in that interval can still allow a new external upload or publication. PR497 remains Draft and undeployed. The owner must add a fresh persisted tenant/site gate immediately before every irreversible provider call, close already-created attempts without calling the provider when revoked, and retain real external outcome evidence when revocation happens only after the provider call. Deterministic race regressions are required for ordinary publish, video upload and video publish. No real customer, provider, publication, collection, database or migration action ran.

## Latest production checkpoint — supersedes older state below

- 2026-09-08 20:58 GEO background-entitlement release completed. Reviewed PR493 exact `f7bbfe7611c4f8cbfd3aa3b9435183cb969e243e` merged to production-geo as `63fefec53906c99d78c3d800395af44afd72d1e1`; deployment `34228329337` and baseline `34228329402` succeeded. Active backend/frontend releases are the `/opt/.../20260908T125145Z-63fefec53906` pair; previous/rollback are the `/opt/.../20260908T111003Z-bbbb216be5c8` pair; `migration=not-run`. Public `/geo-health` is 200/db=ok across scheduler-owner and standby workers, dashboard and current JS/CSS assets are 200. The deployment health retry observed five expected localhost connection refusals during process restart before success; there was no rollback, database or scheduler error. No customer, task14, generation, patrol, publication, metric rebuild or business call ran. H2-H4 human acceptance is now gated only by an authorized H1-ready content task and a removable non-production publication channel.
- 2026-09-08 20:58 SEO disabled-action Draft PR497 exact `9f9b60207a4b98f09144bf20a4aec14213aa3108` on production-seo `a5a5747120cb0c783342ccf9b7918e53cf265490` is blocked at first coordinator review P1=1. Its global gate checks site operational state only when `site_id` is directly present in the request; resource-ID writes such as content submit-review, review and update load the real `SeoContentAsset.site_id` later and currently commit without checking that site, so disabled-site business actions remain possible. The owner must produce a complete resource→site write-entry matrix, enforce the real loaded resource scope in handlers, and narrow the inactive evidence/stop-control allowlist by actual semantics. PR497 remains Draft and undeployed; no customer or external SEO action ran.

- 2026-09-08 20:48 GEO background-entitlement PR493 final exact `f7bbfe7611c4f8cbfd3aa3b9435183cb969e243e` on production-geo `bbbb216be5c8b3823bb98c269ac1c57ea4f55dd3` passed coordinator review P1=0/P2=0. The final change rolls back connector-side credential refresh and other pending writes when entitlement expires during a remote send, then persists only a sanitized `unknown` delivery audit in a fresh task→variant locked transaction if the reservation is still owned; a concurrent human recovery is not overwritten. Independent `ops/run_geo_checks.py` passed 1085 tests with 47 skips, diff check is clean, and all exact-head PR checks are green. The GEO owner is authorized to perform an unchanged exact-head controlled production-geo merge/deploy and return release/rollback/health/scheduler/log evidence. No customer, task14, business call, migration or H2-H4 action is authorized during deployment.

- 2026-09-08 20:44 usage rule changed by the user: continue development, review, merge and controlled deployment while the Codex primary allowance has at least 30% remaining; when remaining falls below 30%, immediately stop/interupt active work, send no new assignment, preserve exact state, update this handoff and notify the user. Heartbeat `automation-2` has been updated to the same five-minute rule. Never consume a reset credit without explicit user authorization. This supersedes every older 60% or 20% threshold below.

- 2026-09-08 20:28 GEO PR493 third review: exact `65572aa5868544afc705a1cf54331cb34df98772` added per-target publish rechecks, entitlement-gated nightly metrics, publication monitoring and outcome review, plus a seven-entry background execution policy and a complete H2/H3/H4 human acceptance plan. Independent `ops/run_geo_checks.py` passed 1084 tests with 46 skips. It remains blocked P1=1 and undeployed: a social connector may flush a refreshed OAuth/WeChat credential during the remote call, then the after-send revocation branch commits the intended `unknown` delivery audit and unintentionally commits that credential mutation too. The owner must rollback the business transaction, reload/lock the delivery reservation in a new transaction, and persist only sanitized unknown audit evidence without overwriting a concurrently recovered reservation; regression must prove the credential remains unchanged. H2 approval-version invalidation, H3 real-platform idempotency and H4 published-body equality still require human acceptance after a final reviewed GEO SHA is deployed; they are not cancelled. No human action is requested yet.

- 2026-09-08 20:05 SEM compatibility production release completed in the required order. Backend PR495 exact `ae378ce033199ce5adb7daa23a9789b9fcb99dd6` merged to production-sem-backend as `b2bfe8cc603b221348dd684b21f40d4f804e452b`; workflow `34223476800` succeeded, active `/opt/sem-backend/releases/sem-b2bfe8cc603b-20260908T115904Z`, previous `/opt/sem-backend/releases/sem-697eecde61af-20260908T111525Z`, `migration=not-run`, and both public health endpoints report the exact commit with `db=ok`. Only after that health evidence, frontend PR496 exact `3df52cdff4fddfd6b3b24aae42fe9661763946b1` merged to production-sem as `c4ef62be7cb042a8985189e8bd9eb6d9632fff69`; workflow `34223769577` succeeded, active `/opt/sem-frontend/releases/20260908T120229Z-c4ef62be7cb0`, previous `/opt/sem-frontend/releases/20260908T111752Z-74fa8a76a48e`, and public cockpit plus exact `AcquisitionCockpitView-CO7TE8yN.js` return 200 with the compatibility fields and conservative unknown-ownership copy. No sync,投放,回写,migration or customer-data write ran. SEO and GEO expiry/background audits remain active. Current primary Codex usage is 34% used / 66% remaining.

- 2026-09-08 19:57 active lanes: SEM compatibility production-sync Drafts #495 backend exact `ae378ce033199ce5adb7daa23a9789b9fcb99dd6` on `697eecde61afce0ace45913c2dbdcceb1ff8ba5f` and #496 frontend exact `3df52cdff4fddfd6b3b24aae42fe9661763946b1` on `74fa8a76a48e084b4fd17cb0c6b0fa56ef6fc084` passed coordinator review P1=0/P2=0. Both production bases remain exact, PRs are clean and CI is green; independent focused verification passed 50 backend tests and 30 client plus 10 summary contracts. The SEM owner is authorized to merge/deploy #495 first and only after healthy backend evidence merge/deploy #496; no sync,投放,回写,migration or customer write is allowed. GEO worker-entitlement PR493 moved from `c34fdb1a...` to `4c33847f4bfd3bd9f589f464d4480a540d20ee63` and fixed the two rollback-expiration findings, but remains blocked P1 because async multi-channel push can continue subsequent real publications after mid-flight revocation, nightly daily-metric rebuilds ignore entitlement, and publication-monitor/outcome-review schedulers still write for expired tenants without an explicit audit-preservation policy. It remains Draft and undeployed while the GEO owner completes the full background-entry matrix and tests. SEO has begun a module-only audit of the same expiry/disabled-site boundary across its schedulers, queues, collection, generation and publication paths; no customer operation is authorized. Current primary Codex usage is 33% used / 67% remaining, above the 60% stop line.

- 2026-09-08 19:40 follow-on lanes: SEO page-evidence PR492 exact `f080c38434f6105adb6ab0c78ab0a4c8d781bfcf` passed coordinator review P1=0/P2=0, merged to production-seo as `a5a5747120cb0c783342ccf9b7918e53cf265490`, and production workflow `34221266058` plus baseline `34221266744` succeeded. It preserves pre-publication crawl facts but marks them stale and never treats them as post-publication verification; no crawl or publication was triggered. The SEO owner is collecting exact release evidence. SEM rolling-compatibility PR494 exact `33f599af2b713bfe607112736d6ba6d08efbc634` passed coordinator review P1=0/P2=0 and all CI, then merged to main as `8323b7cfa4fbb87a93aa3298af55daf61b769780`. It retains a conservative three-state legacy surface while adding strict evidence status/count fields, and the new consumer hides unsafe legacy NULL-account matched metrics. Separate backend/frontend production-sync Drafts are being prepared from current releases; no further SEM production merge/deploy has occurred yet.

- 2026-09-08 19:19 SEM/GEO releases completed. SEM backend PR490 exact `ee0d955943f82c6c4383d558a79066323ac4bb64` passed production review P1=0/P2=0, merged as `697eecde61afce0ace45913c2dbdcceb1ff8ba5f`, and controlled workflow `34219630900` succeeded; active release is `/opt/sem-backend/releases/sem-697eecde61af-20260908T111525Z`, `migration=not-run`, and public `/health` returns that exact commit with `db=ok`. SEM frontend PR491 exact `e14e694bccebdbad79aa03f4d33a4518a45a7b7e` passed review, merged as `74fa8a76a48e084b4fd17cb0c6b0fa56ef6fc084`, and workflow `34219839714` succeeded; atomic deployment timestamp is `20260908T111752Z`, and public `/workspace/cockpit` plus current `AcquisitionCockpitView-BbrwZb-A.js` are 200 with the new unknown-ownership evidence copy. GEO PR488 final exact `dbb0ac34f187779ee16c3ff0d7289a76df4e36ab` passed review P1=0/P2=0 and all CI, merged as `bbbb216be5c8b3823bb98c269ac1c57ea4f55dd3`; production workflow `34219108434` and baseline `34219108478` succeeded. Active GEO backend/frontend releases are the `/opt/.../20260908T111003Z-bbbb216be5c8` pair, previous/rollback is the `20260908T101817Z-a9f0f068b9f0` pair, and `migration=not-run`. Forty public health samples were all 200/db=ok with one scheduler-lock holder and standby workers; dashboard/current assets are 200. No customer, task14, sync, generation, publication or database write ran.
- SEO PR489 post-release evidence is complete: current production is `4bead5da5c4f7f9448dd306b8a74bbfe406d7490`, active backend/frontend releases are the `/opt/.../20260908T110136Z-4bead5da5c4f` pair, public health and current assets are healthy, and `migration=not-run`. The workflow did not print the server `previous` symlinks; the prior successful commit `491f0cc3268445a0e9e303d191274968f143f1ca` is only a rollback candidate until an administrator performs a read-only symlink check. `/seo-openapi.json` remains 404 because platform PR474 is still gated on the final module installation; this does not affect the SEO service itself.

- 2026-09-08 19:05 release progress: SEO PR489 was corrected at exact `7286b649a8fa64cdfb65b919086257b715554216`, passed refreshed coordinator review P1=0/P2=0 and all five CI checks, then squash-merged to production-seo as `4bead5da5c4f7f9448dd306b8a74bbfe406d7490`. Production deployment run `34218400024` and baseline run `34218400534` both succeeded; workflow evidence reports `migration=not-run` and new backend/frontend releases `/opt/seo-service/releases/20260908T110136Z-4bead5da5c4f` and `/opt/seo-frontend/releases/20260908T110136Z-4bead5da5c4f`. Public `/seo-health` and `/seo/` return 200 with db/schema revision `0094_seo_qa_batches` healthy. The SEO owner is collecting previous/rollback evidence; no real platform sync/publication/customer operation ran. SEM PR487 was corrected at exact `97c57c141ac76a24ad503f01c3d99766ab47505a`, passed coordinator review P1=0/P2=0 and all CI, and squash-merged to main as `8539aa3223c485e5cd0bdc176092cceeb22b2749`; separate backend and frontend production-sync Drafts are now being prepared. GEO PR488 second head `d04fe8b79ea73d27a8b4004d621070442944474f` fixed `application/*+json` but remained blocked P1=1 because FastAPI also accepts JSON bodies with no Content-Type; the owner is adding that real-route gate and regression. No SEM/GEO production merge or deployment occurred from these Drafts.

- 2026-09-08 18:53 SEO publication concurrency Draft PR489 exact `33157eca78b7873c0860ad6e61f5a3d65a55d1cd` adds rollback handling for manual publication/QA placement races and records sanitized unknown provider-sync failures. Coordinator review found P1=0/P2=1: all three new handlers currently translate any `IntegrityError` into a duplicate/race response, even when no competing winner exists, which can hide unrelated constraint or database faults. It remains blocked until each path re-reads the expected winner after rollback and only returns the idempotent/409 outcome when that evidence exists; negative no-winner tests are required. No real platform login, customer data, publication, database, merge or deployment action ran.

- 2026-09-08 18:49 active review gates: SEM Draft PR487 exact `decbd3dcc1196127d46902f41afbcd8ef87ac7a3` correctly stops NULL-owned keyword assets from joining NULL-owned reports, but coordinator review found P2=1 because a known asset account plus only NULL-owned same-ID reports is still labelled `account_mismatch` and rendered as “other account”; NULL does not prove another account. It remains blocked pending a truthful report-ownership-unknown state and tests. GEO Draft PR488 exact `be1877349a7f58efe06871663fa9c67e22a60dd8` adds a global entitlement gate, but coordinator review found P1=1: it inspects bodies only for exact `application/json`, while FastAPI accepts `application/*+json`; a vendor-JSON write can therefore bypass the proposed body tenant gate. It also must prove coverage for any write route whose tenant is derived only from a path object or auth context. Both owners are fixing their own Drafts; neither may merge/deploy. No customer, task14, publication, generation, database or production write ran.

- 2026-09-08 18:39 SEO production read-only reconciliation completed without manufacturing a no-op PR. Current `codex/production-seo` `f702c8e7e0c5f21f723eeb7b2513a7124453c6a7` already contains the reviewed site-scope and page-detail capability through merged PR436; focused production tests passed 11/11 and the audit branch has zero changed files. The production page-detail implementation also retains issue details, snapshot comparison and bounded incoming-link evidence that a replay of the current main variant would remove. If runtime behavior later disagrees, verify the immutable deployed SEO SHA/service inventory instead of resyncing equivalent or weaker code. No customer login, site, collection, generation, publication, database, merge or deployment action occurred.

- 2026-09-08 18:36 platform-route lane refreshed after the production SEM frontend advanced: Draft PR474 was cleanly rebased to current `codex/production-sem` `7c9217c1887131ea3af2483788e75b13be84d260` as exact head `d11d1f260765bb73d761dc71a6e65d737c034c23`. The six-file diff is unchanged in behavior, remains mergeable/clean, preserves the reviewed module SHA-256 `d1522668411f34c7329e3777aea2bbf138fc99820baef1c1dc295a22f6ad9a2d`, and all GitHub checks are green. It remains Draft and must not merge until the administrator installs that exact reviewed module from this new exact head and returns the clean checkout, installer backup, enabled status and installed digest. The previously installed `c8824ba...` module remains superseded; no route/Nginx/customer/database operation ran in this refresh.

- 2026-09-08 18:29 completed releases: SEM frontend PR486 merged to production-sem as `7c9217c1887131ea3af2483788e75b13be84d260`; workflow `34214566839` completed build, pytest and controlled frontend deployment successfully. Active release is `/opt/sem-frontend/releases/20260908T101738Z-7c9217c18871`, previous `/opt/sem-frontend/releases/20260908T094245Z-b2a1a21142e4`; public cockpit and its exact lazy chunk are 200 and contain the reviewed `报告关联`, `本页展示` and `仅其他账户有同 ID 报告` markers. Backend and migration were not run.
- GEO PR484 merged to production-geo as `a9f0f068b9f01a3db5a07c50d316938aaafabf11`; production workflow `34214583171` and baseline workflow `34214583362` succeeded. Active backend/frontend releases are `/opt/geo-service/releases/20260908T101817Z-a9f0f068b9f0` and `/opt/geo-frontend/releases/20260908T101817Z-a9f0f068b9f0`; previous releases are the `4a629e8b2e7d` pair. `/geo-health` and the dashboard return 200, `db=ok`, `migration=not-run`, and the current frontend asset contains `sem:auth-context-changed`. Health reports the local scheduler as `skipped` and follow-up scheduler as `standby` because this process does not hold the scheduler lock; continue observation before treating it as an incident. No task14/customer/collection/generation/publication/database operation ran.

- 2026-09-08 18:14 active release lanes: SEM PR485 exact `5cd206da1a23818be3e963dcd39486a1bb15ed9e` made nullable/cross-account keyword report association and paginated list scope visible, passed review P1=0/P2=0 and merged to main as `a1c8cd9d210a688c7a2c6cc566f293c7334b5448`. Production sync Draft PR486 exact `bf6d326866ea2c56dfd483369bbc54d5fa328691` on production-sem `b2a1a21142e4d6e4d4750e07297f159985dc99ef` passed the refreshed production review and is authorized for controlled frontend merge/deploy. No search-term-to-keyword linkage is claimed because the stored search-term model lacks a stable keyword ID.
- GEO production sync PR484 was corrected to exact `af355f75afc602621e7f0644d95d08ce4136c739` on production-geo `4a629e8b2e7de6e42591d37b021d19e4e513e98e`. The coordinator-approved shared session change only emits the existing auth-context notification after setAuth/refreshUser/logout state updates; GEO revalidates the current tab, reloads other tabs on persistent storage changes and preserves public share access. Direct real-session tests and all CI pass. Final review P1=0/P2=0; controlled production-geo merge/deploy is authorized, with no task14/customer/business/database action.
- Usage stop line remains active. Latest primary Codex usage is 32% used / 68% remaining, above the 60% stop threshold. SEM, SEO and GEO next-audit turns are all active; do not duplicate assignments.

- 2026-09-08 17:48 SEO workbench production gate: PR483 exact `757ff619e3230eb07d628c5fb95f4c5bb055af3d` passed coordinator review P1=0/P2=0 and merged to production-sem as `b2a1a21142e4d6e4d4750e07297f159985dc99ef`. Workflow `34211398098` completed pytest, build and controlled frontend deployment successfully. Active release is `/opt/sem-frontend/releases/20260908T094245Z-b2a1a21142e4`, previous `/opt/sem-frontend/releases/20260908T093447Z-2d60c674baff`. Public workbench and its exact lazy chunk return 200 with the new command-center markers. The SEO consumer now rechecks active site scope before data reads; no customer login, site operation, collection, publication or database write ran.
- GEO production sync Draft PR484 exact `b76d94c19184fb50979b16d20ecfa488199cf1d4` failed coordinator review P1=0/P2=1 and must not merge/deploy. Its production fallback listens only to cross-tab `storage` events, while the production session store emits no same-tab auth-context event from `setAuth`, `refreshUser` or `logout`; an already open private GEO route therefore does not immediately re-evaluate when the current tab refreshes a user's permissions. Cross-tab tests do not establish the required same-tab behavior. The GEO owner is preparing a contract-correct proposal and must explicitly surface any shared-session file dependency for coordinator ownership.

- 2026-09-08 17:39 workbench experience release: production sync PR482 exact `d9a2bebb4aa74894829174471f579f1ca3db46c9` passed independent cross-review P1=0/P2=0, 135 cross-module auth/read-only contracts, 31 counted workbench tests plus three script contracts, production build and artifact verification. It merged to `codex/production-sem` as `2d60c674baffc505db11288724196715ea446e71`; workflow `34210651277` completed pytest, build and controlled frontend deployment successfully. Active release is `/opt/sem-frontend/releases/20260908T093447Z-2d60c674baff`, previous is `/opt/sem-frontend/releases/20260908T052646Z-dac8569b5b35`. Public `/workspace/cockpit` and the exact lazy chunk return 200 and contain the reviewed real-time status, action ledger, data-boundary, AI-agent and chat/split/data controls. No backend, migration, customer-data or business write was included.

- 2026-09-08 17:32 release checkpoint: SEO PR481 merged to main as `32b3534500ab061e3ea58804ab69491feb40c4e2`; GEO PR477 then passed the refreshed-base review and merged to main as `7591f1f17db4cfb89eba5c089218bdd8c7c57868`. Neither main merge deployed its module. The workbench experience PR480 was finally rebased to that combined main, passed all focused suites/build/CI at exact `a71f957f76f8b220e3eb9d43730850d192ac7243`, and squash-merged to main as `6f5b5415b4fe8b6521594ff885822fbb11c2214a`.
- SEM production backend PR479 merged as `5e4cb063e5d0ac9fa57a6b378a1984bb3dda6aaf` and controlled workflow `34208579015` completed successfully. Active release is `/opt/sem-backend/releases/sem-5e4cb063e5d0-20260908T091150Z`, previous is `/opt/sem-backend/releases/sem-0386b3c34adf-20260908T043732Z`; public health returns the exact active commit with `db=ok`, the four anonymous cockpit endpoints return 401, and `migration=not-run`. No sync,投放,回写, AI/cache generation or customer write ran.
- Workbench production sync Draft PR482 is open at exact `d9a2bebb4aa74894829174471f579f1ca3db46c9` on production-sem `dac8569b5b35c76e1e1830bb949a1e9c627c98ba`. Its conflict resolution deliberately layers the new interaction on the production branch while retaining production-only module entitlement visibility, fine-grained denied state, stale-scope guards, inclusive GEO week wording and `/seo/site` route. The diff is four workbench frontend/test files; focused workbench routes and contracts, production build and 94-asset verification pass. The SEM owner is performing an independent read-only cross-review; no merge/deploy is authorized before that result and exact CI.
- GEO is preparing a module-only production-geo sync Draft for the six reviewed standalone auth/session files. SEO is preparing a separate production-sem consumer-contract Draft for the five reviewed site-state gate files. Both must preserve their production branch fixes and stop before merge/deploy so the coordinator can order them around PR482.

- 2026-09-08 17:10 coordination pass: the authoritative main branch is now `e04d5c926fd98ce3dcfc12c6e28388caa57bd268` after SEM PR478 merged. The workbench experience branch was rebased to that main as exact `b280ad13231e42f408265cf8b80e8e02c3dae3de`; the focused workbench suites, production frontend build and 106-asset SEM build verification passed, and Draft PR480 is open and all CI is green. It remains unmerged and undeployed while the module main changes land, after which its base will receive one final diff review.
- SEM production backend sync Draft PR479 exact `7a80c118ba382f873a12646712178c3c978b3616` on production base `0386b3c34adfc0e77772b1455024b54565688899` passed coordinator review P1=0/P2=0. It changes only five SEM backend/document/test files and adds explicit matched/account_mismatch/no_report evidence without cross-account data merging. All CI is green. The SEM owner is authorized to merge and observe the controlled backend deployment, returning exact release/health/rollback evidence; no sync,投放 or writeback is authorized.
- SEO final read-boundary audit was rebased to current main as Draft PR481 exact `b795ab3e746ac0a44013b384cc6fc65bb5b067b1`. It rechecks the explicitly selected site through the existing GET-only workbench site-scope route and rejects missing, paused or archived sites before content/page reads. Coordinator confirmed that content-only ordinary identities remain accepted by both auth mapping and endpoint permission checks. Independent review P1=0/P2=0; 67 Node contracts, 147 focused pytest cases and all CI passed. The SEO owner is authorized to merge only to main; production reconciliation/deployment remains separate.
- GEO Draft PR477 was rebased to current main as exact `013e5eef38248a00ec17c3ce2842d886b8de2689`. The prior redirect P2 is fixed: the real conditional administrator query credential `key`, including case variants and hash-route copies, is removed while legitimate tenant scope is preserved. Independent review P1=0/P2=0; 14 auth/redirect/storage tests, shared session routing, production build, diff check and all CI passed. It remains Draft until main advances from the SEO merge and its final base diff is rechecked. No task14, customer, collection, generation, publication or database operation occurred.

- SEM read-chain integrity Draft PR478 exact `efbc50a19ff2b20299bb861db43af31190fff035` on main `021809323e6194c008e4bf2c549d0855a726e48c` restores active-only default account scope on main and exposes keyword report association as matched/account_mismatch/no_report without joining cross-account rows. It preserves explicit historical reads for non-active accounts, null metrics for gaps, `completeness=unknown`, tenant isolation and GET-only behavior. Coordinator independent review PASS P1=0/P2=0; all GitHub checks are green and an independent focused run passed 68 tests. The SEM owner is authorized to execute the unchanged exact-head squash merge to main and then prepare a backend-only production sync Draft PR; no deployment is authorized until that sync receives a second coordinator review.
- GEO session/access audit produced Draft PR477 on base main `021809323e6194c008e4bf2c549d0855a726e48c`, initial exact head `fd9a0e9a079cd7aeb3ea99550e6b6914c5bb67f9`. It adds shared auth-context revalidation to the independent GEO app, requires `geo.content` on private routes, preserves the public share route and sanitizes login redirects. Coordinator review reproduced one P2: the sanitizer removed several credential names but retained the actually supported conditional administrator query key `key` in both shell and hash queries. The owner is fixing it with regression coverage. Initial review P1=0/P2=1; PR477 must not merge or deploy until a new exact head passes re-review. No customer, task14, collection, generation, publication or database operation occurred.
- 2026-09-08 workbench experience parity lane: `codex/workbench-experience-parity-20260908` exact `f013e460d59d6c8b991f71beeaa4605abeebf486` restores the customer-facing AI conversation as the primary control surface and adds chat/split/data layouts, module filtering, compact/expanded cards, fullscreen fallback, action ledger, explicit data-boundary view, motion and 320px responsive behavior. It preserves the existing GET-only SEM/SEO/GEO integrations, tenant/module/menu/site isolation, stale-response invalidation and missing/no-data/unavailable-vs-zero semantics. Independent final review passed P1/P2/P3=0. `npm run build`, `npm run verify:sem-build`, the relevant workbench/SEM/SEO/GEO contract suites and Playwright interaction smoke passed. The branch is pushed but no PR exists because GitHub CLI is unavailable and browser automation could not verify the current Windows browser URL; production therefore still serves the prior cockpit layout. Compare/create-PR entry: `https://github.com/DoraHai/ai_sni/compare/main...codex/workbench-experience-parity-20260908?expand=1`.
- 2026-09-08 16:10 coordination restart: the existing tasks `SEM 获客工作台只读数据接入`, `SEO 生产开发续接 2026-09-05` and `1.0` (current GEO lane) were explicitly restarted with strict module-only scopes. SEM is auditing the production SEM-to-workbench read path; SEO is completing all non-human work around PR475 and the SEO evidence/publication boundary; GEO is auditing session/access/current-brand/publication gates and H2-H4 automation without operating task 14 or customer data. The coordinator owns cross-module contracts, workbench code, review, merge/deploy order and this file.
- Platform PR474 remains Draft and clean at exact `aeff623de7e5f4e0697754a3665ba33204b1963a`; SEO PR475 remains Draft and clean at exact `474e19642f827cf7a84585886676a6814e37777f`. The server administrator previously installed superseded platform module hash `c8824ba23eb0efdd57f9c6a0027685f3d2da7a99d39a09cef54d8e639493639d`; PR474 still requires final reviewed module hash `d1522668411f34c7329e3777aea2bbf138fc99820baef1c1dc295a22f6ad9a2d` before merge-triggered apply. No Nginx change, route deployment, SEO production login or customer operation is authorized by this checkpoint.
- Usage stop line is active again per the latest user instruction: stop development, review, merge and deployment when the primary Codex allowance falls below 60% remaining. Latest verified remaining allowance is 76%, so work continues.

- 2026-09-08 current continuation: workbench SEM detail PR473 exact `7a76938a53e7f1d922c3dbf782f133511d5acaf6` passed final independent review P1/P2/P3=0 and merged to main as `021809323e6194c008e4bf2c549d0855a726e48c`. Production sync PR476 exact `975c397025b79d7b8ebaa5072ef66263f350a50b` passed review and merged to `codex/production-sem` as `dac8569b5b35c76e1e1830bb949a1e9c627c98ba`; workflow `34190569304` completed pytest, frontend build and controlled deploy successfully. Active release is `/opt/sem-frontend/releases/20260908T052646Z-dac8569b5b35`, previous `/opt/sem-frontend/releases/20260908T043947Z-7657ea927ea1`, and current public cockpit/asset are 200 with the new account-scope, keyword and search-window markers. It uses only existing GET contracts, visibly degrades older account-scope contracts, preserves missing-vs-zero, and invalidates the entire concurrent detail batch on permission loss. No backend, migration, writeback or customer-data operation ran.
- Platform retry PR472 exact `9be3bbf7eb16c334cfc8fc06c101a69e4bc9b721` passed independent review but was closed unmerged after SEO required the same route module. Combined Platform PR474 exact `aeff623de7e5f4e0697754a3665ba33204b1963a` on production-sem `dac8569b...` passed independent review P1/P2/P3=0 and all CI. It preserves the reviewed bounded network retry and fail-closed locked double live-head boundary, and adds only exact GET `/seo-openapi.json` to SEO port 8020 with service-schema smoke; root `/openapi.json` remains unchanged. It remains Draft pending administrator installation of final module hash `d1522668411f34c7329e3777aea2bbf138fc99820baef1c1dc295a22f6ad9a2d`. SEO harness PR475 exact `474e19642f827cf7a84585886676a6814e37777f` also passed independent review P1/P2/P3=0 but remains Draft until PR474 is deployed. Tiger user6 exists with the five required SEO permissions; no credential or customer business probe ran because the route is not deployed yet.
- GEO PR468 final exact `016553da49a5ef8694ff8ea2d7c89cd0dd5375ed` passed independent review P1/P2/P3=0 and all CI, then merged to production-geo as `4a629e8b2e7de6e42591d37b021d19e4e513e98e`. Baseline workflow `34192376562` and production deployment `34192376579` succeeded with `migration=not-run`; active backend/frontend releases are `/opt/geo-service/releases/20260908T055501Z-4a629e8b2e7d` and `/opt/geo-frontend/releases/20260908T055501Z-4a629e8b2e7d`. Public `/geo-health` and dashboard return 200. The deployed endpoint enforces body-only tenant-bound ordinary identity, shared tenant locking and atomic/idempotent disabled foundation creation without collection/generation/publication. No production foundation call or customer write has been authorized or executed.
- Development resumed on 2026-09-08. Tiger remains three-module entitled but only SEM is data-ready. SEO PR467 exact `ac494eab7aed769f90da73770bb1b4589b0eec3a` passed its prior independent review plus the portable-checksum delta review and all exact checks, then squash-merged to main as `1cb50088ccd3f1c343e49fb572db9b366f33fcec`. It authorizes no production login, site creation, collection or publication. GEO Draft PR468 first failed independent review P1=1/P2=0 because a point-in-time eligibility result was not mutual exclusion. The owner replaced that plan with shared tenant locking, scheduler post-lock settings recheck and one atomic safe-foundation operation; exact `0265f8c448935ed3401cf88240d21edb06b94a5a` has green CI and is in independent re-review. No Tiger GEO foundation write is authorized yet. SEM's active-only account and client-completeness repairs are now reviewed, merged and deployed as recorded below; the next cockpit-owned work is the visible account/unassigned warning and keyword-to-search-term drilldown. No customer-data, collection, generation, publication or authenticated production action occurred in these resumed lanes.
- Auth production deployment permission was restored by the GitHub administrator. Failed run `34139848896` was rerun as attempt 2 and completed successfully on exact production-auth merge `4a46544a8f74d128931d2522abcafbe99ba837f2`. Current release is `/opt/auth-frontend/releases/20260908T041932Z-4a46544a8f74`, previous is `/opt/auth-frontend/releases/20260818T201048Z-verified-ui`, backend/migration/service restart were not included, and public `/login`, `/workspace/cockpit` and `/health` returned 200.
- SEM PR469 exact `fae7be8e5a7a9aa585367d845d877cc96d4b3c91` passed independent review P1=0/P2=0 and all checks, merged to production-sem-backend as `0386b3c34adfc0e77772b1455024b54565688899`, and controlled deployment run `34187603720` completed successfully. Active backend release is `/opt/sem-backend/releases/sem-0386b3c34adf-20260908T043732Z`, migration was not run, and public health reports the exact commit with db=ok. SEM PR470 first failed review P2 on accepting zero-row search windows, was fixed at `6ec56bdac363596183326a3451fa9858135a92be`, passed re-review P1=0/P2=0 and all checks, merged to production-sem as `7657ea927ea1a765194739a101d1db8bdae5cf28`, and CI/deploy run `34187725482` succeeded. Active frontend is `/opt/sem-frontend/releases/20260908T043947Z-7657ea927ea1`; previous is `/opt/sem-frontend/releases/20260908T042624Z-59775a0640b9`.
- The old platform-route run correctly failed its stale-head guard because production-sem had advanced. PR471 exact `889ab64bc8ee3897a2d2f26f599b58064ff6e4e8` passed independent review P1=0/P2=0 and merged as `59775a0640b91293e1fe5aa0723327a2d76a5746`. Route run `34186917030` passed verify but both deploy attempts failed closed before archive/config/Nginx mutation because the server-side authoritative `git ls-remote` received GitHub HTTP/2 EOF. Public `/platform*` routes remain 404 and `/workspace/cockpit` remains 200. A bounded server-side network retry fix is now being prepared; no further blind rerun is allowed before review of that fix.
- Tiger tenant 4 three-module readiness is now classified from read-only evidence: SEM has one real current account `12` with 837 keyword assets, 1,297 daily rows through 2026-09-07, 167 search-term rows, 3,825 region rows and 1,726 weekday/hour rows; archived empty account `4` must not enter the default scope. SEO trial is open but no production Tiger site/site_id or production content/page/publication/GSC evidence is yet established; the public preflight has 26/26 reachable pages and remains explicitly non-production. GEO trial is open but business/profile, three prompts, answers, formal complete-week metrics, content tasks, engines/channels and publications remain empty. Opening modules is not data readiness; only SEM is currently data-ready.
- Tiger offline safety work passed exact-SHA cross-review and merged to main without deployment or production access: GEO empty/insufficient/null and safe-foundation contract PR463 merged as `107ea37dabe315787223ce457fe7b28382a7def6`; SEM search-window completeness PR418 merged as `52bf584501e9594dbda44132ae4c0ab358b44070`; SEO fixed-origin, GET-only, tenant-4, minimum-permission and active-only acceptance harness PR464 merged as `8ba839703d91e7720bf8160b249d61ed3c704ef8`. GEO stale Draft PR391 was closed as superseded. SEM read-only production sync PR457 exact `adc578b3378c62e2331021ae55e000f623472703` passed independent review P1=0/P2=0 and merged to production-sem as `f9a4d211732ec059b66fc14ffaa756516d62df5e`; post-merge CI passed without deployment. Independent backend PR466 exact `246794abd9271ca16e31c172d09ca5b7121a98ae` then passed review P1=0/P2=0, merged to production-sem-backend as `2aa1ff3556f5d3e3d0c2b1c5bebb75fcfdd3ef04`, and controlled workflow `34182571816` completed verify+deploy successfully. Active backend release is `/opt/sem-backend/releases/sem-2aa1ff3556f5-20260908T031107Z`, `migration=not-run`; public health reports the exact commit and db=ok, cockpit is 200, and anonymous probes of the three list endpoints are 401. No sync, writeback, customer write, authenticated login, collection, generation or publication occurred.
- Platform-route PR461 merged to `codex/production-sem` as `aa67fbf4ff955669321a334a75f1e1d498806f79`. Its first controlled deploy run `34176672209` passed verify, installed and validated the candidate, then received one immediate public `/platform` 404 after Nginx reload and automatically restored the reviewed base config; production stayed on the old route config. Root cause was the smoke check racing a draining old Nginx worker. Fix PR462 exact `d5b45cdb4f061456bff92b59e9542ccf348a3080` added a bounded ten-attempt exact-shell readiness loop and a Linux transient-404 regression, passed all exact checks, and merged as `fd5946bede1e9cd76a6227004d60fbdc15275a1b`. The next deploy failed closed before publication when the server's final GitHub live-head query hit an HTTP/2 transport error. Failed-job retry reached the host, but the host still has the old root-owned module from `859f90b4`, so it again performed the old single-shot smoke and rolled back. Administrator must update only `/etc/platform-deploy/modules/platform` via the reviewed installer from exact `fd5946be...`; expected installer SHA-256 remains `d3dc2eb5d3cf11ed0d4633e1979977fa58052030e4acf1ff0210ef9766599d52`, expected new module SHA-256 is `bca5188c661ae4991c0b36dce451f3367af9cdb4e24a1d0d8bad8a2a82a41829`. After evidence of `platform=enabled`, backup path and exact installed hash, rerun only the failed route deploy job `34177468866`, then verify `/platform`, `/platform/customers`, `/platform/accounts`, `/platform/roles`, old SEM routes, legacy redirect, health and active/backup config hashes. No database, customer or migration action occurred.
- PR461 pre-merge production gate confirmed exact Draft/base/head/checks and a successful read-only connection run `34146475506` / job `101819509758`, but host 101 reports `platform=not-configured`. The PR was correctly left Draft and unmerged so its push cannot enter apply without the module. Administrator bootstrap is now required from exact commit `859f90b41d29cb2bccec6ff651afa85514fcddc9`: installer `ops/platform-deploy/install-platform-routes.sh` SHA-256 `d3dc2eb5d3cf11ed0d4633e1979977fa58052030e4acf1ff0210ef9766599d52` and sibling module `ops/platform-deploy/modules/platform` SHA-256 `212e5a04bef8e4c5b46fd709bfbd26b76e23089353934b8ec20e75524dc3008e`. Run only the reviewed installer with `--enable`, then return backup/module hash and dispatcher status showing `platform=enabled`. No manual Nginx edit is authorized. After that evidence, recheck exact PR state before merge/deployment.
- Nginx route PR461 exact `859f90b41d29cb2bccec6ff651afa85514fcddc9` received complete independent review PASS, P1=0/P2=0, against base `f9a51c65e8f900130e08e0f7c4fcb7dbe03d1e5a`. All seven infrastructure files and 24 Linux tests were rechecked. The reviewer confirmed locked double live-head authorization, strict inert/retained archive validation and byte-equivalent idempotent reuse, zero trust in runner authorization, retry/anomaly/rollback safety, and the single ordered Nginx location with unchanged existing route precedence. Exact CI is green; there are no application/backend/frontend/migration/customer changes. The owner is authorized to merge and run only the dedicated controlled platform-routes deployment, with reviewed installer/module gate and full config/route/rollback evidence; no manual config or authenticated business write is allowed.
- Nginx route PR461 archive-idempotence candidate is now exact `859f90b41d29cb2bccec6ff651afa85514fcddc9`; prior heads are invalid. Under the server lock/live authorization, retained archive reuse now reports strict regular/non-link/nlink/owner/size/SHA/safe-entry/manifest/candidate and byte-equivalence checks, followed by a second independent live-head query at the final boundary. Exact match deletes only the run's inert part, preserves the archive inode and continues current smoke or base apply; anomaly preserves the archive and stops before config/Nginx mutation. Tests report successful same-commit rerun without reload, post-publish smoke rollback then reuse/apply success, archive anomaly refusals, and drift between the two server queries. Linux route verification reports 24 passed; full CI/build are green and deploy jobs skipped. GEO is performing a complete exact-head re-review; PR remains Draft/undeployed.
- Nginx route PR461 exact `85a1e6fb6c0881e1b0325a97b5bf108f7dead75f` failed the next review P1=1/P2=0 and remains Draft. Server-side live-head authorization is now accepted as a safe linearization boundary, but the fixed commit archive is retained after success or post-publish rollback and a same-commit rerun rejects its existence before reaching already-current smoke/reapply. This would require manual server deletion after a transient failure. The required fix is strict idempotent reuse: under lock/live authorization, validate an existing archive's type, non-link status, owner, size, digest, safe entries, manifest and candidate identity; on exact match remove only the new inert part and continue smoke/apply, while any mismatch fails closed and preserves the existing archive. Linux tests must cover successful same-commit rerun, post-publish failure/rollback then successful reuse, and mismatched/link/ownership/type archive refusal with zero active/Nginx mutation. No merge/deployment occurred.
- Nginx route PR461 final-review candidate is now exact `85a1e6fb6c0881e1b0325a97b5bf108f7dead75f`; prior heads are invalid. The restricted server module now accepts only a unique inert upload path, installs exit cleanup, holds the deploy lock through validation/authorization/publish/apply, and independently performs strict exact-ref `git ls-remote` against the public production-sem branch. The runner no longer publishes the archive or supplies an authorization value. Only one valid exact candidate match permits archive publication and config mutation; network/newer/malformed/multiple/missing results delete the inert part and stop before archive/config/Nginx/smoke activity. Reported anonymous live query returned the current production-sem SHA. Linux route verification reports 16 passed, covering successful boundary, runner drift, server failure/drift/parse cases, ordered rollback and four rollback failures; full CI/build are green and deploy jobs skipped. GEO is performing a complete exact-head re-review; PR remains Draft/undeployed.
- Nginx route PR461 exact `f215faf800cf424a1fd2f6914f4260bca912e642` failed second independent review P1=1/P2=0 and remains Draft. The earlier rollback P1/P2 are fixed, including explicit restore-step failures, status 70, ordered Linux state-machine assertions and restore failure injection. Remaining P1: the runner's final branch read still precedes SSH; a new push in that gap lets the old run pass two old values to the server, and async cancellation is not a safety boundary. The owner must move authoritative branch-head verification into the trusted server-side lock immediately before any inert-part publication, rename, active-config mutation or apply; the server must query the public production-sem ref itself and fail closed on network/parse/ref mismatch. Tests must cover drift after the runner's final read, live-query failure/malformed/multiple refs and current-head success, proving no archive/config/Nginx mutation on failure. No merge/deploy occurred.
- Nginx route PR461 review-fix head is now `f215faf800cf424a1fd2f6914f4260bca912e642`; all prior heads are invalid for release. The workflow now reports ref-scoped cancellation, authoritative branch-head checks before upload and after inert `.part` upload, one remote publish+apply command, and module-side authorized-head equality. Deterministic drift tests claim no upload/SSH before-upload drift and one inert upload/no SSH after-upload drift. Rollback steps explicitly propagate install/move/digest/`nginx -t`/reload failure, require reviewed-base digest, avoid reload after invalid restoration and return 70 on cleanup failure. Linux tests assert ordered apply/rollback events and inject restore install/move/validation/reload failures. Reported route verification is 11 passed with CI pytest/build green and deploy jobs skipped. GEO is performing a new complete exact-head review; PR remains Draft with no deployment.
- Nginx route PR461 exact `8fd4b1eb1fd17df45c6a7d51ab0dc598b21321ae` failed independent review P1=2/P2=1 and remains Draft/undeployed. P1: the workflow's branch-head check occurs before artifact/SSH/upload, so an older non-cancelled run can still apply after a newer push; it needs an authoritative exact-head recheck immediately before all remote mutation plus deterministic stale-run evidence. P1: `restore` is called under `if ! restore`, which disables Bash errexit behavior inside the function and can let install/move/`nginx -t` failures be masked by a later successful reload; every rollback step must explicitly propagate failure, invalid config must never reload, restored bytes/digest must be checked, and cleanup must return status 70 on rollback failure. P2: the Linux test checks call counts but not ordered apply/rollback sequence and lacks rollback-failure injection. The owner received these exact fixes. The unrelated local GEO brand-profile test failure is evidenced unchanged on base/head and environment-isolation related; it is not permission to edit GEO in this PR.
- Nginx route PR461 review head advanced to `8fd4b1eb1fd17df45c6a7d51ab0dc598b21321ae` on the same production-sem base. It adds a Linux executable rollback test that runs the real module state machine in temporary roots, forces public-smoke failure, asserts byte-for-byte active-config restoration, and asserts `nginx -t` plus `systemctl reload` each run twice. Reported GitHub verify is five passed; pytest and frontend build succeed; deploy jobs remain skipped. Any conclusion for prior head `70ff4d6d...` is invalidated. GEO was instructed to restart/extend the complete review for the new exact head; no merge or deployment is allowed before that result.
- Nginx `/platform` route repair is ready as Draft PR461, base `codex/production-sem@f9a51c65e8f900130e08e0f7c4fcb7dbe03d1e5a`, exact head `70ff4d6d575339b416d564f908ac18e588ad2935`. The reported scope is limited to the exact active config fixture, a candidate adding one `/platform(/|$)` SEM SPA location, restricted platform deploy module, one-time installer, dedicated workflow and contract tests. It claims pre-write unknown-config refusal, archive traversal/link/special-file rejection, config backup, `nginx -t`, reload, public smoke and full rollback, including already-current smoke. Exact PR checks are green and deploy jobs skipped. Local full suite reports 640 passed/two skipped plus one frozen GEO mock target mismatch; the independent reviewer must classify that failure rather than infer from green CI. GEO has been assigned the complete exact-SHA review; no merge/deploy/migration or production action occurred.
- Usage stop line changed by the user: compute remaining Codex development allowance as `100 - codex.usedPercent`; immediately stop/interupt all development, review, merge and deployment work when remaining is below 60%, preserve the exact work state and do not use a reset credit. At this checkpoint the authoritative usage read returned 7% used / 93% remaining, so development may continue. Heartbeat `automation-2` now enforces this threshold every five minutes.
- Platform production sync PR460 merged as `f9a51c65e8f900130e08e0f7c4fcb7dbe03d1e5a`; controlled workflow `34142471431` completed successfully for pytest, frontend build and deployment. Active/current is `/opt/sem-frontend/releases/20260907T161727Z-f9a51c65e8f9`, exact release marker matches the merge, and retained rollback package is `/opt/sem-frontend/releases/20260907T131916Z-05fcb20ac250` / `05fcb20ac2504c6cd49ae7930417ae0cb29bc020`; the deploy script records the prior target but does not create a `previous` symlink. Public index/current bytes match, all 19 direct assets and three platform chunks are 200, `/workspace/cockpit` is 200, and `/health` remains backend `88649dd20b0fe939c153e029ec007d6bb27f0cf9` with db=ok. No backend or migration ran.
- Platform route acceptance found a production blocker after the healthy frontend release: direct `/platform/customers`, `/platform/accounts` and `/platform/roles` return Nginx 404 because both active and tracked `deploy/gsnipers.conf` omit `platform` from the SEM SPA route allowlist. Rollback would not resolve this pre-existing config omission. No manual server edit was made. The owner is preparing a separate minimal Nginx route/config-validation/atomic-deploy/rollback Draft from current production-sem; it must receive a new independent review before any controlled config deployment.
- SEO PR458 merged to `codex/production-seo` as `f311dca7e1a2339dce8d071ad6e6494d612a948c` after unchanged exact-head/base/check revalidation. The post-merge baseline ran all five jobs successfully. Both production deploy workflows correctly skipped by path filters because the merge contains only workflows, documentation, release verification and the contract test; no application/frontend/migration path changed. No server apply, release-link change, health revalidation, customer access or production-data write occurred.
- Platform production sync PR460 exact `9aa1ddaf280d98b8a3f6aa93a51c67c3e3c437d2` now has fully green exact-head CI. The job log proves the two platform-admin native PostgreSQL concurrency tests passed with zero skips; the full production-sem selection reports 637 passed and two unrelated skips. Base remains `05fcb20ac2504c6cd49ae7930417ae0cb29bc020`, Draft remains true, and the independent full-diff review is still in progress.
- Platform PR459 merged to main as `62f12ed7fdc49d06428f382f47232c04de7b8e3a` after exact-head gate recheck. Production-sem sync Draft PR460 now targets base `codex/production-sem@05fcb20ac2504c6cd49ae7930417ae0cb29bc020` at exact head `9aa1ddaf280d98b8a3f6aa93a51c67c3e3c437d2`. Its reported diff is exactly the 19 reviewed platform-admin/prerequisite/test/workflow files, preserving the production dynamic cockpit shortcut and deployment behavior; no migration/deployment occurred. Local evidence: 637 passed with two PostgreSQL-only skips, focused 49 with the same two skips, frontend build/UI contracts/artifact verification and YAML/diff checks passed. The production workflow adds disposable PostgreSQL for both no-skip concurrency races. GEO has been assigned a complete exact-SHA review; PR460 remains Draft.
- SEO PR458 current exact head `e9161392ab596979de499a7efa0bf47a309ac9c7` against `codex/production-seo@2927dea41982399bad4af2391194a7d551e0040f` received independent complete review PASS, P1=0/P2=0. The five-file diff is limited to SEO workflows, release verification, PostgreSQL onboarding contract test and Tiger handoff documentation; `app/**`, production frontend source and migrations are unchanged. The native PostgreSQL migration-validation path ran without skip and passed. The owner is authorized to merge and report any workflow/deployment effect; production Tiger site creation, Nord site1 access, collection and publication remain prohibited during that merge lane.
- Platform PR459 exact `f01087f22098dd30edf62de6b44aa606491d3bee` received full independent re-review PASS, P1=0/P2=0, against base `a0d848d9de94712d082528db4d7f7c11470d479f`. The reviewer rechecked all 19 changed files, confirmed the shared transaction lock closes user/role/cross-entry write skew, confirmed both real two-session PostgreSQL races, the corrected self-profile guard and backend-authoritative availability, and found no migration or scope expansion. Exact CI run `34140964059`, GEO baseline `34140963646` and GEO read-only PostgreSQL run `34140963729` succeeded. The owner is authorized to merge PR459 to main, then prepare a separate minimal production-sem sync Draft; production deployment still requires a new exact-SHA review of that sync.
- Platform PR459 owner completed the three review fixes at new exact head `f01087f22098dd30edf62de6b44aa606491d3bee`, still against frozen base `main@a0d848d9de94712d082528db4d7f7c11470d479f` and still Draft/undeployed. The user/role mutations now share one stable PostgreSQL advisory transaction lock acquired before reading/recomputing/mutating full-admin state, with real two-session races for concurrent user and role demotions. The accounts-only self-profile guard and frontend UTC expiry recomputation were also corrected. Reported evidence: full local 2141 passed/60 skipped, focused 56 passed, frontend build/contracts/artifact verification passed, exact-head GitHub 11 success plus one expected skip and no failures, including native PostgreSQL tests. The original reviewer has been assigned a complete exact-head re-review; no merge is allowed until it passes.
- Auth PR449 received a fresh attributable independent review from the SEO task at exact base `eaa2c93c6ddb839e2bfdf4acabb06eb2910cd01e` / head `67d8bd2666dd1e3e3f45861636a1ff072a076895`: PASS, P1=0/P2=0. It merged to `codex/production-auth` as `4a46544a8f74d128931d2522abcafbe99ba837f2`; workflow `34139848896` passed exact verification but the deploy job was rejected before runner allocation because GitHub Environment `production` does not allow branch `codex/production-auth`. Host 101 is unchanged at `/opt/auth-frontend/releases/20260818T201048Z-verified-ui`, previous remains empty, and installer backup remains `/var/backups/platform-deploy/auth-20260907T152955Z.c1EyGX`. The available credential received GitHub 403 on environment policy edit. Repository administration must add the exact branch rule, after which the failed deploy job can be rerun without a new merge.
- Platform PR459 exact `970fd177cf2cb6ef48e9faa3760d5c54449f81a6` failed independent review P1=1/P2=2 and remains Draft. P1: last-full-admin checks in user/role mutations are vulnerable to two-transaction write skew; use one stable transaction serialization lock before recompute+mutation and add real two-session PostgreSQL tests. P2: an accounts-only administrator's ordinary self-profile update is incorrectly rejected by an unconditional full-admin postcondition. P2: the frontend recomputes expiry with UTC browser date and can disagree with the server during Asia/Shanghai 00:00–07:59; consume server-authoritative availability/effective status. The owner has been assigned these exact fixes; no merge/deployment/migration occurred.
- Review provenance correction: neither the SEO nor GEO owner had performed the earlier summarized independent re-review of Auth PR449 exact `67d8bd2666dd1e3e3f45861636a1ff072a076895`, and GitHub had no review/comment record. That earlier statement was excluded from the release gate and caused the fresh attributable review now recorded above; this did not invalidate the successful dispatcher installation.
- Platform administration phase 1 is implemented as Draft PR459, branch `codex/platform-admin-center-phase1-20260907`, frozen base `main@a0d848d9de94712d082528db4d7f7c11470d479f`, exact head `970fd177cf2cb6ef48e9faa3760d5c54449f81a6`. It adds the independent `/platform` shell and customer/account/role routes, safe legacy redirects, global entry, removes system settings from the SEM sidebar, and adds the bounded backend authorization/lockout/reset/module-update semantics described in the design. No migration or deployment occurred. Reported validation: full pytest 2140 passed/58 skipped, target 55 passed, frontend contracts/components/build/artifact verification and diff check passed. GEO has been assigned a separate exact-SHA independent review; the PR remains Draft.
- Latest H1 human result: the current-brand recheck entry/action passed. The case still correctly remains not-ready on `howto_extractable`: intent `场景` maps to a how-to structure and requires an evidence-supported steps block. No waiver is recommended; no agent may edit or re-run task14. A later content-side revision, if explicitly authorized, should add supported steps and run one recheck.
- Auth dispatcher installation gate passed on host 101. Administrator returned clean `HEAD=67d8bd2666dd1e3e3f45861636a1ff072a076895`, `auth=enabled`, current Auth release `/opt/auth-frontend/releases/20260818T201048Z-verified-ui`, no previous release because this is the first dispatcher enablement, and backup `/var/backups/platform-deploy/auth-20260907T152955Z.c1EyGX`. This authorized the final exact-head/base/CI/review check, merge to `codex/production-auth` and controlled Auth deployment. The explicit first-deployment rollback baseline is `verified-ui`; no manual production-file edit is allowed.
- GEO completed the requested Tiger read-only foundation handoff without code or production action. Draft document: `D:/SNIPERS国内版/ai_sni-worktrees/geo-tiger-safe-foundation-20260907/docs/cockpit/GEO_TIGER_TENANT4_SAFE_FOUNDATION_DRAFT.md`. It fixes the confirmed business/profile fields, the three exact questions, official website channel as `manual_only` plus `enabled=false`, and bans unit creation, tracking-engine/settings enablement, patrol, generation, channel accounts and publication. It also records per-object duplicate checks and the important limitation that business/prompts lack a public hard-delete recovery path, so uncertain creates must stop before POST.
- Administrator reported the production superadmin session is open in the browser. Coordinator read-only verification reached `https://gsnipers.snipers.com.cn/workspace/cockpit`; the page stayed authenticated and currently displayed tenant `诺德`, but showed zero available modules and no read timestamp. This is evidence that the production page is reachable under the session, not a completed tenant/module acceptance. No form, customer setting or business write was submitted.
- Auth installer source is now pinned for the administrator: repository `git@github.com:DoraHai/ai_sni.git`, branch `codex/production-auth-safe-redirect-20260907`, exact commit `67d8bd2666dd1e3e3f45861636a1ff072a076895`, script `ops/platform-deploy/install-auth.sh`. The administrator is authorized to clone that exact branch into a new isolated staging checkout on host 101, verify detached HEAD/clean status/script existence, and run `install-auth.sh --enable`. Existing checkouts must not be reset or modified by Git. The installer must stop without modification if the installed dispatcher digest is outside its reviewed allowlist, and return sanitized installer/status/current/previous/backup evidence.
- SEO contract-test PR458 final exact `d62c8552c87a667279015e27eb3483d0c04e66f8` now preserves real `require_scoped_auth`, covers missing SEO permission and tenant-query conflict before database access, and uses a real isolated PostgreSQL `AsyncSession` test with canonical foreign keys/unique constraint for create/readback, duplicate 409, handler rollback/no pollution and cross-tenant rejection. Related local suite: 52 passed and one database test skipped because the local test database URL is absent; the baseline migration-validation environment ran it without skip and reported 173 passed. All five exact-revision CI checks passed. The PR remains Draft, changes only test infrastructure/contracts, and has no production code, merge, deployment or migration effect.
- Current task cadence: SEM/Auth and SEO tasks are active; GEO is idle after the latest bounded handoff. GEO remains prohibited from commit/push/deploy/production/task14 operations and has been asked only to prepare a read-only Tiger onboarding checklist. SEO was asked to prepare the exact Tiger site duplicate check/minimum fields/no-collection verification without creating it. SEM/Auth received the administrator's exact isolated-checkout installation instruction.

- Coordination heartbeat `automation-2` now inspects the three module tasks and this authoritative record every five minutes, remaining quiet without material change and notifying on completion, failure, production risk or required human/admin/database action.
- GEO owner violated the Draft/review/deploy boundary again by pushing and deploying production-geo commit `437638b442e6d03d254f4f3e53d92b5f53ef6ffe` directly from parent `d0e2fea69d9e4880b9d25b4356eced43479c81a2`. The net diff is only `GeoTaskEditorView.vue` plus its surface test (24 additions): when an existing master article has no brand warning payload, it exposes a stable “按当前品牌重新检查” action that calls the existing `runCheck`. GEO frontend 183/183, baseline `34137375926` and production `34137375931` succeeded; migration=`not-run`; public release marker is exact, `/geo-health` and dashboard are 200. Coordinator performed an after-the-fact independent diff review and found P1=0/P2=0, so the healthy release is retained rather than rolled back. The process deviation is explicit; GEO is stopped from further commit/push/deploy/production/task14 action. One human H1 click is now required on task14; the agent must not perform it.

- Platform administration is now a first-class product lane. The user correctly identified that customer/module provisioning and account/role/password administration must not live under SEM. Design is recorded in `docs/cockpit/PLATFORM_ADMIN_CENTER_DESIGN.md`: global `/platform/customers`, `/platform/accounts`, `/platform/roles` and later `/platform/audit`; top-right global gear; old `/settings/*` redirects; logged-in built-in administrator must retain both `settings.accounts=edit` and `settings.customers=edit`; tenant-bound accounts cannot manage customer modules; passwords are reset/invited but never viewable. Phase 1 reuses current APIs and requires no migration; forced first-change/session revocation and multi-selected-tenant users remain later reviewed data-model work. A separate implementation assessment was assigned after the current SEM release repair and must not be mixed with SEM/Auth changes.

- The user selected `SZ-老虎新材料` as the intended full-module integration customer. Production read-only verification identifies the unique tenant as `tenant_id=4`. SEM is active with no expiry. The real account baseline is `baidu_account_id=12` (active OAuth subaccount, synced through 2026-09-07); archived empty account `4` must not be treated as a second active account. Account 12 has 837 keyword assets, 1,297 keyword-report rows over 2026-07-27..2026-09-07, 167 search-term rows for 2026-08-08..2026-09-07, 3,825 region rows and 1,726 weekday/hour rows. Completeness remains unknown. SEO and GEO have no entitlement for tenant 4 and therefore no legally selectable tenant-scoped data. GEO has zero prompts, answers, real/simulated/manual samples, formal metrics, tasks, engines, channels or publications. SEO production UI exposed a cross-customer stale-state defect: after switching to unentitled Tiger, it correctly reports SEO unavailable but retains Nord's selected site and 321-page display. SEO was assigned a tenant/entitlement/site invalidation Draft fix; SEM was assigned a Draft fix so default cockpit account scope excludes archived accounts. No entitlement, permission, configuration, collection, publication or database write has run. To start real SEO/GEO data, administrator must first provision tenant 4's module entitlements; the user still needs to supply/confirm Tiger's official website domain and the intended trial/active expiry policy.
- The user confirmed Tiger's official website as `https://www.tiger-coatings.cn/`; use it as the sole canonical SEO domain and GEO website/docs own-domain candidate. Public site evidence identifies TIGER/老虎, TIGER Drylac® powder coatings and surface technology (plus industrial digital-printing inks), and the application groups transportation, architecture/building materials, general industry and green lifestyle, with examples including curtain walls, system windows/doors, automotive wheels, furniture/appliances and machinery. Module provisioning is still required before any production site/profile/engine/question configuration. The default recommendation remains 30-day trial through 2026-10-07 unless the user chooses active or a different expiry.
- Tiger module provisioning completed through the supported admin API, not direct SQL. For `tenant_id=4`, SEO is `trial/available=true`, opened `2026-09-07T22:36:18.970338`, expires `2026-10-07`; GEO is `trial/available=true`, opened `2026-09-07T22:36:19.744574`, expires `2026-10-07`. Superadmin `/api/v1/auth/modules` reports both available. SEO was authorized to create the single confirmed official site through the formal API after duplicate check, without collection. GEO was authorized only for a write-safe preflight and disabled/non-scheduled base objects; any automatic collection risk must stop the write. No collection, generation, publication or direct database write is authorized at this stage.
- SEM archived-account scope PR456 exact `ba2d2e3ce1eef1c30bf1db116a3af914d672f5f4` passed independent review P1=0/P2=0 and merged to main as `a0d848d9de94712d082528db4d7f7c11470d479f`. Default dashboard/keyword/detail/region/hour/phone/search-term scopes consistently exclude archived accounts, while explicit same-tenant archived history remains readable and labelled; unknown/cross-tenant IDs are 404 and unassigned data stays in a separate bucket. A minimal production-sem sync Draft is being prepared; the fix is not yet in production.
- SEM production sync PR457 exact `181940446dea09be6a26b0d9dba9f7b7214a838d` failed independent review P1=1/P2=1 and remains Draft. Although 13 non-workflow files matched main, whole-file prerequisite copies also brought classic SEM list/sync/writeback behavior and cross-SEO/GEO auth changes beyond the read-only scope; the deployment test list also omitted a currently runnable shared permission test. The owner must port only cockpit route/scoped-auth hunks or explicitly widen scope with proportional tests. No merge or deployment occurred.
- SEM PR457 was narrowed on new exact `c290f373034166d642a3971949a2ad8da3feba2f`: dashboard/keywords/search-terms now contain only Request/reader imports and the four cockpit GET routes, `app/security/auth.py` is identical to production base, and the deployment gate adds `test_module_workspaces.py`. Cockpit/module 70, exact gate 251, full Python 690, Node contracts 134, consumer 25 and build/verify passed; second independent review is in progress. Still Draft and undeployed.
- SEO Tiger site creation remains blocked only by production application identity: anonymous GET `/api/v1/seo/sites?tenant_id=4` returned expected 401, so no duplicate check or POST ran. SEO contract-test PR458 first exact `912d33297d4dd6eb48b2a268374a4cab9f02ae6b` failed review P1=0/P2=2 because it bypassed real scoped auth and used a hand-written session to overclaim database transaction safety. It remains Draft; the owner is replacing those claims/tests with real auth and PostgreSQL transaction evidence. No production write occurred.
- SEO cross-tenant stale-state repair PR455 required three review rounds. Final exact `874bf22727bc0131d04e02b5b071f84d8d088349` passed independent review P1=0/P2=0 and merged to production-seo as `2927dea41982399bad4af2391194a7d551e0040f`. The shell uses the entitlement-aware `/api/v1/seo/workbench/sites`, clears site/route/subpage state on tenant/auth changes, rejects late responses, only selects statuses allowed by the backend policy, and prevents direct URL/back/forward navigation to paused or archived sites. Baseline run `34135205853` and frontend production run `34135205337` completed successfully, including exact-version verification and deployment. Post-release verification passed: active `/opt/seo-frontend/releases/20260907T145205Z-2927dea41982-frontend`, previous commit prefix `491f0cc32684`, public `/seo/` and all 50 current assets return 200, and the deployed shell imports the workbench-sites transport rather than the assets-management transport while containing selectable-status route reconciliation and `no-active-site`. No migration, login or customer data operation ran.

- One/two/three-module cockpit release is deployed. Shared identity PR452 exact `80cc4c1cec55e8e43571e050cdaf8c7eb60b9ace` merged to main as `476afb0d4c3df5d44d878472299a4dd368bdc352`. Production SEM backend sync PR454 final exact `be67f1a6fb00a908099b04b9736df5351a88b722` passed independent review P1=0/P2=0 and the exact 241-test production selection; it merged as `88649dd20b0fe939c153e029ec007d6bb27f0cf9`. Manual production workflow `34126428699` completed successfully for both exact-package verification and deployment. Public `/health` returns 200 with `release_commit=88649dd20b0fe939c153e029ec007d6bb27f0cf9` and `db=ok`. Cockpit frontend PR451 final exact `442fcdbd9b58607c756ea9c0772d23fb385eb94e` passed independent review P1=0/P2=0 and merged as `05fcb20ac2504c6cd49ae7930417ae0cb29bc020`; CI/deployment run `34126666919` completed successfully for pytest, `sem-frontend-build` and `Deploy SEM frontend only`. Public `/workspace`, `/workspace/cockpit`, `/health` and the current `AcquisitionCockpitView-C5dK2b9c.js`/`ModuleWorkspaceView-Ct5IlObW.js` assets return 200; the current cockpit asset calls `/api/v1/auth/modules` and contains explicit unavailable-state handling. The product behavior is fixed: any customer with at least one available SEM/SEO/GEO module enters `/workspace/cockpit`; the cockpit renders exactly the available one, two or three modules, hides unopened/expired/other-tenant modules, and leaves opened-but-data-denied modules visible with explicit unavailability rather than fabricated zeroes. Auth default routing remains pending PR449 review/deployment, so an explicit safe redirect or existing route may still determine the immediate post-login landing until that separate Auth release is complete.

- SEO publication-to-page evidence is deployed. Draft PR453 fixed exact `d4a4ce9135ec49a31576e6e8b927459b6cf30590` passed independent review P1=0/P2=0 and merged as `491f0cc3268445a0e9e303d191274968f143f1ca`. The GET-only endpoint requires both `seo.content` and `seo.site`; URL association only normalizes scheme/host case, default ports and fragments while preserving trailing slashes and raw query distinctions, and it reports incomplete inventory instead of guessing an exact unique page. Baseline run `34126887788` passed all five checks and production deployment `34126887264` passed exact-commit verification and deployment. Public `/seo-health` returns 200. Post-release active/previous paths, migration status and unauthenticated fail-closed probe are being collected; no customer identity, publication or business write was used.

- Auth PR449 final exact `67d8bd2666dd1e3e3f45861636a1ff072a076895` on base `eaa2c93c6ddb839e2bfdf4acabb06eb2910cd01e` carries the implementation and test evidence summarized here, but the claimed independent-review provenance was later found missing and a new exact-SHA review is required before merge. The fixed server lock covers unpack, old-link read, install, switch, online acceptance, commit and complete rollback; native Linux concurrency tests report that a second candidate fails without leaving a release. The installer verifies canonical-LF SHA-256 before any directory, backup or write, allows only reviewed-base→candidate or candidate idempotence, and leaves an unknown dispatcher unchanged. Redirect behavior remains: safe redirect first, otherwise any available SEM/SEO/GEO module routes to `/workspace/cockpit`, with zero modules or API failure falling back to `/workspace`. The dispatcher is now installed and enabled, but PR449 remains Draft and undeployed until the newly assigned independent review passes.

- GEO H2-H4 access/read-side repair PR450 final exact `031b7c3bbd71af42df5a45895ab3fe0482cca272` passed independent review P1=0/P2=0 and merged/deployed as `d0e2fea69d9e4880b9d25b4356eced43479c81a2`. Baseline `34124768412` and production deployment `34124768455` succeeded with migration=`not-run`. Active backend and frontend are `/opt/geo-service/releases/20260907T125944Z-d0e2fea69d9e` and `/opt/geo-frontend/releases/20260907T125944Z-d0e2fea69d9e`; both current symlinks point there. The host has no independent previous/rollback symlink, but retained release `20260907T111753Z-7fee77e50ef2` / SHA `7fee77e50ef277c6f6c0a83c733b07738833d538` is the verified rollback candidate. `/geo-health`, dashboard and eight direct assets are 200; db=ok; service active/running; no error-priority journal entries after startup. Server current hashes for `tenant_scope.py`, `content/routes.py` and `content/multi_push.py` match Git blobs byte-for-byte, proving active/trial/expiry gates, pre-execution recheck and pure-read zero-account push-target behavior are deployed. No authenticated business call, customer write or task #14 operation ran.

- Product rule confirmed by the user: every customer with at least one active SEM, SEO or GEO module enters `/workspace/cockpit` after login, including one-module customers. The cockpit must contract to exactly the purchased/available module set: one, two or three modules; unopened/expired modules are hidden and are not counted as unresolved. Module availability must come from `/api/v1/auth/modules` plus the selected tenant's module scope, not from the prior six data-card permissions alone. An opened module with insufficient fine-grained data-read permission remains visible with an explicit permission limitation and no fabricated values. `/workspace` remains the module selector and safe fallback. Auth PR449 will use the module availability response for default routing; a separate production-sem Draft PR will align route visibility, module filtering and single/double/triple layouts. No merge or deployment has occurred for this rule yet.
- Production-sem Draft PR451 first exact `9acaa898...` was blocked because the shared backend omitted legitimate SEO permissions `seo.alerts`, `seo.links` and `seo.competitors`. Shared identity PR452 exact `80cc4c1cec55e8e43571e050cdaf8c7eb60b9ace` added all eight registered SEO leaves plus active/trial/expired/unopened/no-permission/bound-tenant tests, passed review P1=0/P2=0, and merged to main as `476afb0d4c3df5d44d878472299a4dd368bdc352`. A two-file sync Draft from current production backend `3cadafae1cab7b8380f243c865305ca08d0dd386` is now being prepared; it must deploy before the cockpit frontend. PR451 final exact `442fcdbd9b58607c756ea9c0772d23fb385eb94e` synchronizes the full mapping and preserves entitlement-based one/two/three-module rendering, passed re-review P1=0/P2=0, but remains Draft and blocked on the production backend sync/deployment.

- Entry replacement is complete. The 2026-09-07 audit found `/workspace/cockpit` live while main-shell, diagnosis-center and GEO shortcuts still routed “全域驾驶舱” to `/deal-sniper/hub/dashboard`; SEO's independent production build had no such entry. Main-shell PR446 first exact `3737cab...` was blocked because users without cockpit permissions could see a dead link. Final exact `4ed9b7467fcbe0cc7f18cf69c70e327da57a6ba1` introduced one shared six-permission cockpit predicate used by router and both entry surfaces, passed re-review P1=0/P2=0, and merged/deployed as `ba6ad6da8aefa73e809ad28c856df498e1984f17` via workflow `34114905431`. Post-release verification at `2026-09-07 19:10:07 +08:00` confirmed active `/opt/sem-frontend/releases/20260907T110732Z-ba6ad6da8aef`, previous `/opt/sem-frontend/releases/20260907T103406Z-1bfdd88764fa`, relevant routes/assets 200, new label/path in current bundles, and the legacy URL still compatible. GEO PR447 first exact `75c948...` was blocked for the same `geo.assets`/`geo.diagnosis`-only dead-link risk. Final exact `db72cf1ab3efb3518a04b128574879d278c276a5` added the same six-permission semantics inside the GEO boundary, passed re-review P1=0/P2=0, and merged/deployed as `7fee77e50ef277c6f6c0a83c733b07738833d538`. GEO deployment `34115742196` and baseline `34115742179` succeeded with migration=`not-run`; `/geo-health`, dashboard, main bundle and `GeoWorkspaceShell-Cvbj45ib.js` are 200, and the production chunk contains `G‑Snipers 获客工作台`, `/workspace/cockpit` and the permission keys without the old hub target. GEO rollback is `a2d5d3a57010cce7159c2e7b48cd62e7b6d3fd92`. No customer data or task #14 was touched.
- Portal semantics are now explicit. A user screenshot exposed that `/deal-sniper/portal` was still an iframe over the old static product prototype, so it displayed “全域层 / 驾驶舱” even after authenticated entry links had moved. Portal PR448 was split from auth work after first review found an open-redirect risk and confirmed auth is a separate deployment unit. Portal-only final exact `6efb456c2fdb10ab4977b4cbe4fc9ae759038723` changed only the public portal and its test, passed re-review P1=0/P2=0, and merged/deployed as `862e9bbed02af350800a7fb57e00b8ff8c60824f` via workflow `34117634009`. Active is `/opt/sem-frontend/releases/20260907T113856Z-862e9bbed02a`; previous is `/opt/sem-frontend/releases/20260907T110732Z-ba6ad6da8aef`. Public `/deal-sniper/portal`, `/deal-sniper-prototype/index.html` and `/workspace/cockpit` are 200 and all 19 directly referenced portal-shell assets are 200; the portal contains the formal G-Snipers workbench CTA, four module entries and AI conversation/data/urgent/action-ledger language, and contains none of `全域层`, `全域驾驶舱`, `进入全域层`, `原型 Demo` or `静态原型`. The product hierarchy is: public product portal, authenticated module selector `/workspace`, and core acquisition workbench `/workspace/cockpit`. Auth default-login routing and strict same-origin redirect validation remain in a separate Draft/auth release lane because `/login` is served by `/opt/auth-frontend/current`, not the SEM frontend deployment.
- Auth Draft PR449 on baseline `codex/production-auth@eaa2c93c6ddb839e2bfdf4acabb06eb2910cd01e` remains blocked. First exact `537082549ca3752912cb88e606934afa60708adf` was blocked P1=1/P2=2 for missing live-candidate identity binding, incomplete workflow triggers and incomplete previous-link rollback. Second exact `e29ea83b04f25840a9730b5f63c5c864a262eee5` fixed those and passed the login rule itself: safe redirect wins; otherwise any available SEM/SEO/GEO module, including a single module, enters `/workspace/cockpit`; no module or module lookup failure falls back to `/workspace`. A second independent review still blocked release P1=1/P2=1 because the server Auth apply state machine lacked a mutex outside GitHub Actions and the installer could overwrite an unknown/newer shared dispatcher. The owner is adding a server-side lock covering the complete current/previous/install/health/rollback window, real concurrent Linux tests, and digest-gated base→candidate/idempotent installer behavior with zero modification on unknown dispatcher digests. The publicly served Auth artifact matches the repository frozen snapshot after newline normalization, but no historical source commit reconstructs that exact artifact; this provenance gap remains explicit. PR449 stays Draft; no administrator installation, merge or deployment is authorized until a new exact SHA passes independent re-review.

- SEM metric-integrity review lane is resolved. Initial PR441 merged to main as `129d191...`, then two later reviews found extra-precision, Python/JavaScript half-tie and finite-overflow edge cases before any production promotion. Forward fix PR444 exact `72cf75a439f87815e3af8322cb0d0fd3b16b695c` passed final review P1=0/P2=0, including 40,000 cross-language rounding samples, and merged to main as `15cb3ca5b547c33d67bc6f943b689fdbaa196f34`. Production promotion PR445 exact `3a9d9aaec74f1da43dc5fb7e366e98fcf52b9231` passed review and merged as `1bfdd88764fa85be76f1aa90289b39de6bbd1cc5`; workflow `34112041066` succeeded. Active SEM frontend release is `/opt/sem-frontend/releases/20260907T103406Z-1bfdd88764fa`, previous is `/opt/sem-frontend/releases/20260907T090037Z-7b46105e8234`, and public cockpit/original SEM routes are 200. Only the final reviewed two-file state reached production.
- SEO publishing-chain lane is resolved. PR443 was held through two review rounds that found stale observation, stale idempotent replay and content/placement TOCTOU risks. Final exact `37fb03521d523e8a1dbef08c2a609638cd3b90c3` added latest-observation enforcement, current/current-1 exact replay rules, content→placement lock order and PostgreSQL two-session regression tests, then passed final review P1=0/P2=0. It merged to production-seo as `23a24f35713843eacacf9e09c4e4ac1d595d70ae`; backend workflow `34111975773`, frontend `34111975685` and baseline `34111976166` succeeded, migration=`not-run`, `/seo-health` and `/seo/` are 200. No real platform publication ran.
- SEM multi-account storage-key diagnostic PR442 exact `8b85e781994a5b7fbbe7d36f7b23e223a2bf3d87` passed documentation review P1=0/P2=0 and merged to main as `eaa2c93c6ddb839e2bfdf4acabb06eb2910cd01e`. It records read-only detection SQL and migration design questions; it does not authorize production SQL, schema change, resync or customer-data inspection.
- GEO H1 entry fix PR440 exact `463176ccc6f311c72f014468ed41e3504491add6` changed only `GeoTaskEditorView.vue` and its surface test, passed independent review and CI, and merged as `a2d5d3a57010cce7159c2e7b48cd62e7b6d3fd92`. Production deployment `34108446211` and baseline `34108446152` succeeded with migration=`not-run`; `/geo-health` and the dashboard are 200, and public `GeoTaskEditorView-aahe2vpH.js` contains both the brand-configuration-updated notice and current-brand recheck action. Rollback is `f694ddbdeba8026271860089a3d16cfd534b8627`. Process deviation: the GEO owner merged and deployed after the strict boundary assigned merge/deploy control to the coordinator. The production result was independently verified, and the owner was instructed to stop future PRs at draft/exact-SHA handoff until the coordinator explicitly merges. No task #14 or customer write was performed.
- SEO post-release audit PR439 exact `f0480a4a495f6693abdf51f0acbe8e3d0dd23438` passed independent review P1=0/P2=0 and all five SEO checks, then merged to `codex/production-seo` as `129228c42f2725995781683c05e35cfd06ad63d3`. Production backend workflow `34107855568`, frontend workflow `34107855429` and baseline `34107856440` succeeded. Deployment explicitly reported `migration=not-run`; `/seo-health`=200 with env/db/schema ok and schema still `0094_seo_qa_batches`; `/seo/` and its current assets are 200. The new placement-candidates route returns 401 before data for an unauthenticated synthetic tenant/site probe. No customer data or publication was touched. The prior production `e0d04cf...` remains the rollback candidate; the protected workflow did not expose the new active/previous filesystem paths publicly.
- Workbench complete-week copy follow-up PR438 exact `4cf90655a96d57e0eebd194bd3f7162b1a67a53f` passed independent review P1=0/P2=0 and CI, then merged to `codex/production-sem` as `7b46105e823420f74a5c845d4d7ea31b18ad7977`. Production workflow `34103548869` succeeded for tests, build and frontend deployment. Active release is `/opt/sem-frontend/releases/20260907T090037Z-7b46105e8234`; rollback release is `/opt/sem-frontend/releases/20260907T084412Z-01a49b27ee35`. Public `/workspace/cockpit` and the new `AcquisitionCockpitView-B3bCFpkU.js` return 200. Both the GEO complete-week cards and ready reply now express exclusive 2026-09-07 as inclusive 2026-09-06. No module backend, database or customer data was touched.
- Workbench production frontend PR437 was independently re-reviewed at exact head `de80c703b3006c264dc553b81ae15303724a11cc`: PASS, P1=0/P2=0. Required `pytest` and `sem-frontend-build` checks passed. It merged into `codex/production-sem` as `01a49b27ee352e05c2a5466c93b75571701e06a9`.
- Production workflow `34102095820` completed successfully: `pytest`, `sem-frontend-build` and `Deploy SEM frontend only` all succeeded. Active release is `/opt/sem-frontend/releases/20260907T084412Z-01a49b27ee35`; previous/rollback release is `/opt/sem-frontend/releases/20260907T064845Z-afcc63c270e9`.
- Coordinator and SEM-owner public no-auth smoke passed for `/workspace`, `/workspace/cockpit`, `/monitor/dashboard`, `/optimize/keywords`, `/optimize/search-terms`, `/login`, favicon, the index assets and the lazy workbench chunks. HTML is no-store/no-cache and hashed assets are immutable. `AcquisitionCockpitView-Sjbh7YYi.js` and `ModuleWorkspaceView-DUYsG2N-.js` both return 200, contain `/seo/site`, and do not contain stale `/seo/sites`. Public `/DEPLOYED_GIT_COMMIT` is intentionally not exposed and returns 404; exact deployment identity is evidenced by the controlled workflow's release marker check, active release path and matching public chunks.
- SEO backend PR436 exact `22ff3f6bc67693d069e76e94dd22d0e4b7e475d3` passed independent review and all five checks, then merged into `codex/production-seo` as `e0d04cf37f6deb6241038fc0c41b579acaacd1fa`. Production deployment workflow `34100854396` and baseline `34100855025` succeeded, migration=`not-run`, public `/seo-health`=200 with `env=prod`, db/schema ok. Active backend/frontend releases are `/opt/seo-service/releases/20260907T083042Z-e0d04cf37f6d` and `/opt/seo-frontend/releases/20260907T083042Z-e0d04cf37f6d`; rollback backend is `20260906T064845Z-5ef5bab99b5c` and rollback frontend is `20260907T073656Z-e220900567fd-frontend`.
- The production workbench now carries real read-only SEM, SEO and GEO integrations. It is no longer the fake-data prototype. Authenticated tenant/module/menu/data acceptance remains separate from the public shell smoke and must not be claimed until performed with an authorized test identity.
- GEO H1 human retest results have been delivered to the GEO owner. GEO must interpret them and continue H2-H4 automation/static work without operating task #14 or waiting for another human action.
- The user removed the temporary usage stop line. Continue development, review, merge and controlled deployment without pausing at 20% remaining. Database changes, real-customer writes and human-only acceptance still require a concrete request and explicit coordination.

## Operating instructions

- 2026-09-09 00:32 H2 customer-review unblock completed. Human evidence on prior GEO production `63fefec53906c99d78c3d800395af44afd72d1e1` showed `nord_geo_review` received HTTP 403 from `POST /api/v1/geo/content-tasks/14/review` while task 14 was pending at V5/article 22. The first PR499 implementation was held because it let every tenant-bound `geo.content:view` role perform the write. Final exact head `cf8c43391c9451af9f8c0f0091d78a32f7bc5054` restricts decisions to the system `品牌方客户` role with a signed-in tenant-bound identity and `geo.content:view`; custom read-only roles remain denied, customer decisions require current article ID plus task revision, and all editing/generation/channel/publication routes still require edit. Independent focused validation passed 33 Python and 10 frontend tests; PR checks `backend-tests`, `pytest` and `frontend-build` passed. PR499 merged to `codex/production-geo` as `03aed0477b21bbd7145e92b2b6797979baf6b58d`; baseline run `34251383120` and deployment run `34251383171` succeeded. Active release reported by the controlled workflow is `/opt/geo-service/releases/20260908T163151Z-03aed0477b21` with the matching frontend release; `migration=not-run`. Public `/geo-health` is 200 with `db=ok`, and the GEO dashboard is 200. No role/permission JSON/database/task/publication operation was performed by the coordinator. Human next step is limited to a read-only `/api/v1/auth/me` check for `role_label=品牌方客户` and the 诺德 tenant, then exactly one approval click if both match; return HTTP/page state/time and stop before H3/H4.
- 2026-09-09 00:51 H2 customer review production retest PASS. On production `03aed0477b21bbd7145e92b2b6797979baf6b58d` / release `20260908T163151Z-03aed0477b21` / frontend `index-mvqpZxMd.js`, `nord_geo_review` performed exactly one approval against 诺德 task 14 V5/article 22. `POST /api/v1/geo/content-tasks/14/review` returned HTTP 200 and the page changed to `客户已确认`. No publication occurred. H2 may proceed only to the planned minimal-edit review invalidation and channel-draft version consistency check; publication, publishing-account configuration and H3/H4 remain prohibited until H2 is closed.

- Keep the workbench and the three module tasks moving independently. Use exact-SHA review, cross-window review and green CI before merge. Use only controlled repository workflows for deployment.
- Module-owner boundary is strict. SEM owns SEM code, SEO owns SEO code, and GEO owns GEO code. Each owner develops and tests only its assigned module and reports directly to the coordinator. An owner must not edit another module, declare another module complete, coordinate database/admin/human work, or widen a production release without an explicit coordinator assignment.
- The coordinator owns cross-module contracts, workbench integration, task assignment, review assignment, merge order, deployment coordination, human/database/admin requests and the authoritative continuation record. A module owner reports a cross-module finding as evidence and a proposed contract; the coordinator decides where the fix belongs.
- Cross-review is allowed only when explicitly assigned by the coordinator. A reviewer reports P1/P2 findings against an exact SHA and does not take ownership of the reviewed module or make unrequested edits.
- Every module handoff must name: objective and boundary, branch/PR, exact head/base/merge SHA, files changed, tests and CI, review result, deployment/current/rollback state, unresolved items, dependencies, and any requested human/database/admin action. Separate verified facts from assumptions and pending checks.
- Preserve the product boundary: discovery/read evidence and explanation/action are separate. Never turn missing, stale, simulated, site-level or estimated data into customer facts.
- Database work and human tests require a concrete request to the user first: exact object, operation, expected result, prohibited actions and evidence to return. Work that does not need either must continue.
- Inspect the three tasks about every five minutes. Routine report about every fifteen minutes; material completion, failure or production risk is immediate.
- Stop all development, review, merge and deployment work when the Codex primary allowance is below 30% remaining. Preserve exact task state and notify the user. Never consume a reset credit unless the user explicitly asks. Older threshold records are historical and superseded by the 2026-09-08 20:44 instruction.

## Production state

- SEM backend: PR430 deployed as `3cadafae1cab7b8380f243c865305ca08d0dd386`, workflow `34093369715`, verify job `101651452893`, deploy job `101651564257`. Public `/health`=200, exact release and db=ok. Rollback `554934d6cbb2921f10f1fe165cbaf62f5b2544f8`.
- SEM frontend: PR427 deployed as `afcc63c270e9bcffe08d0295a416902ba6594a42`, workflow `34092436470`, deploy job `101648692353`. Coordinator independently rechecked `/health`=200 with backend `554934d6...`/db=ok and `/onboarding`=200 with the app shell. Rollback `ee3bf6c9077b99836a95f607abd44f2a9ac99264`.
- GEO: current production is PR434 merge `f694ddbdeba8026271860089a3d16cfd534b8627`, deployment workflow `34097293229` and baseline `34097293203` succeeded, migration=not-run. `/geo-health`=200, db=ok, both schedulers running and dashboard=200. It retains the PR428 final gate fixes and adds current-brand recheck for the existing V3 without regeneration.
- SEO frontend: PR419 exact `a083b1687612883a5e488d4e8b0660ba84898149` passed independent review and five checks, merged as `e220900567fd191704bd6b44c5957cb10fb2d531`. Production SEO frontend workflow `34096271870` and baseline workflow `34096272471` succeeded. Post-release server/rollback evidence is being collected by the SEO owner.
- Formal workbench is now on main through GEO summary PR435 merge `d93acd8bde4cd1cfdb73c980dd1d75d99f32f5a0`. It is not yet in SEM frontend production.

No migration was run. Coordinator/module tasks did not operate GEO task14, approve content, publish content or perform new real-customer writes.

## Active release and repair lanes

### GEO production hotfix — highest priority

Independent review of production merge `eb1e05d...` found and reproduced these blockers:

1. `app/geo/content/gate.py` recomputes current Markdown brand state but a stale stored `task.rule_result.brand_validation=false` can still block corrected current content. Current recomputation must be authoritative; old metadata may be display/audit evidence only.
2. Manual publication validates before `_write_publication`, but the final locked write does not fresh-read current article and brand state and rerun the hard gate. A concurrent brand configuration change can pass the earlier check and still create a publication record.
3. Variant generation reads brand state before async model work and saves/marks ready from that old state. A concurrent brand change during generation can leave stale-ready variant metadata.

GEO fixed all three on exact reviewed head `4da1183dcbd5b47e0b15cd945c7e0765ffefb6a9`, with current content/brand authoritative, final locked publication revalidation, and post-generation/pre-save variant revalidation. Independent review passed P1=0/P2=0. Evidence: `1029 passed, 43 skipped`, frontend build, PR CI and real PostgreSQL double-session concurrency checks. It merged/deployed as recorded above. Do not operate task14 from an agent. H1 now needs one human retest on this exact production release.

### SEM production sync

- Main PR420 merged as `e79795a03cf3cdc4b49d6497ed20f5b70d8d3038` from exact `973afb94e1ece797faac503a9fa9a050aa48ae0b`; main CI `34091358903` succeeded.
- PR427 exact `8df187a780da0978feb31ac070913bb18de6d97a` passed independent review and CI, merged and deployed as recorded above. It selected the SEM fixes and reviewed auth/session prerequisites, excluding backend, SEO main/router and database work.
- Post-release audit found the backend search-term list lacked account filtering. PR430 exact `a66fa9dfb0019492e4e39587345c368d6507372c` changed only `app/api/search_terms.py` and two tests, passed independent review, 57 tests and CI, then merged/deployed as recorded above. No migration, sync or business write ran.

### SEO

- PR419 merged and its frontend production workflow succeeded as recorded above. It stores the ordinary login/session state atomically, keeps current-tab session priority, synchronizes persistent login changes between tabs and fails closed on old formats.
- Page-detail PR433 exact `ec85f51ce65b4cb49aecc5d73daf6d69c7fc1663` passed independent review, 191 backend plus 51 consumer/transport tests and all CI, then merged to main as `3f83bfb4dbf5f8bf3cc7fd1f8a7a922296b033bf`. It reads stored latest/previous crawl snapshot, link counts and at most 200 incoming sources after tenant+site+page proof. No crawl or write. Main checks passed; it is not yet deployed to the SEO backend production branch.

### Workbench

- PR429 frozen exact `d4f0d249e633657b7f042a7e2f84d742ab6c2085` passed SEO and SEM independent review with P1=0/P2=0 and all CI, then merged to main as `ca3c0fa08b6cdebd5cfa4fc922aa6472c3967f97`.
- Adds real SEO summary evidence for the explicitly selected site: content totals/statuses, review+ready counts, page totals/health/needs-fix, urgent count, and a clearly unavailable single-article-click card. It does not guess the first site, perform writes, trigger collection or infer article clicks.
- Adds module-only view invalidation so SEO permission/site failure clears SEO cards and discussion references without erasing valid SEM evidence. Site/customer/auth revision changes reject late results.
- Independent SEO review of the first head found one P2: business actions and unresolved modules were added together and described as modules. Exact `d4f0d24...` separates the two counts and adds four copy scenarios. Local validation now covers 50 SEO/workbench contracts plus cockpit scope, session, evidence-card and SEM UI suites; production build; `verify:sem-build` over 104 assets; diff check clean.
- The original gap was that a fresh ordinary SEO read-only user could not list/select sites because existing `/api/v1/seo/sites` requires `seo.assets`; the summary slice could only reuse an already selected scoped site.
- PR431 resolved that gap: exact `a42db9050ab45c7319d00e7d255fcb86a1d41cc2` exposed the tenant-qualified GET-only site list and merged as `b3db70ac648e4387047cf58bb29f883f520f393e`.
- Workbench PR432 added the customer-facing site selector and merged as `d1feded223af2b52815db07aac9126c040d0c5cd` from exact `26a266e33c9e8573c654a7a1c19e4a5bfa3aeb97`. Independent SEM review found and drove fixes for two P1 state transitions: invalidated sites cannot silently switch on the second reactive load, and the block remains isolated per tenant across A→B→A switching. Final review PASS P1=0/P2=0; 52 related contracts, frontend build and artifact verification passed.
- Workbench PR435 merged as `d93acd8bde4cd1cfdb73c980dd1d75d99f32f5a0` from exact `72111a00e4ebf01144a01498ae18f065c6a13504`. It mounts the reviewed GEO GET-only transport and GEO-owned authorization context, then shows six complete-week cards: AI mentions, mention rate, visibility score, qualified answers, valid questions and covered engines. Simulated/manual/unknown/excluded/insufficient data remain unavailable and answer rows are not re-aggregated. Independent SEM review PASS P1=0/P2=0; all PR CI passed.

## Ownership and next action

- Current assignments after the production releases: SEM audits only the SEM-to-workbench read path (tenant/account isolation, nullable account ownership, missing-vs-zero, CTR and phone-click coverage); SEO audits only the SEO review-to-placement-to-human-publication flow and automates everything that does not need a real platform login; GEO audits only GEO session consumption, tenant/module access, current-brand recheck presentation, publication preflight and logs without operating task #14. Cross-module findings are returned to the coordinator as evidence, not edited by the module owner.
- Coordinator: keep exact-SHA/CI gates, independently inspect release diffs, maintain this file, and integrate the already reviewed GEO transport, authorization context and formal weekly metrics into the cockpit next. Do not turn the old prototype into production evidence.
- GEO: hotfix the three production review findings, obtain independent re-review, then controlled deploy; afterward interpret H1 result and define any next human test.
- SEO: finish PR419 production post-release evidence and prepare a controlled SEO backend release lane for the main-only site-scope/page-detail endpoints; do not publish customer content or wait on customer image feedback.
- SEM: continue bounded improvements that do not need human writeback; Smart Builder can remain deferred. No new real writeback test beyond the already authorized Open Tiger boundary.

## Human, database and administrator queue

- Human GEO H1 after PR440: authorized tester logs into production, selects tenant 诺德, opens task #14, hard-refreshes once, confirms the notice “品牌配置已更新…” and clicks “按当前品牌重新检查” exactly once. Do not edit or regenerate the draft, approve, create channel copy or publish. Return test time, displayed brand, refreshed GEO score, remaining blocking items, ready/status result, button/notice screenshot, and any console/network error. H2-H4 human flows remain untouched.
- Database: no action now. No schema change is active.
- Administrator: no action now. Production workflows and health endpoints are available; request assistance only if a controlled workflow, credential retrieval or server evidence cannot be completed by the module owner.

## 2026-09-09 full-demo customer decision

- The user requires a presentation-ready customer with rich SEM, SEO and GEO data. Do not mix synthetic SEO/GEO records into Tiger tenant 4 because its SEM history is real.
- Prepare a separate long-lived `G-Snipers 全域演示` tenant and ordinary demo account. Every screen must identify demo data, and every external collection, model run, writeback and publication path must be server-side disabled.
- The coordinator owns the cross-module experience and the execution gate. SEM, SEO and GEO owners may only prepare their own idempotent fixture package, scheduler exclusions and tests; they must not write production data or deploy this scope yet.
- The reviewable design and proposed object volumes are in `docs/cockpit/FULL_DEMO_TENANT_PLAN_20260909.md`. Database execution remains blocked until module audits prove isolation and the user approves the exact transaction/rollback package.
- All three offline fixture packages are now available. Cross-module review rejected direct loading into the production business schema because existing read visibility and scheduler eligibility share active-status gates in SEM and SEO, while GEO correctly excludes simulated data from formal metrics. The recommended implementation is an isolated demo runtime and demo database; exact database and server feasibility questions are recorded in `docs/cockpit/DEMO_ENVIRONMENT_DECISION_20260909.md`. No database or route change is authorized yet.
- Demo runtime audits are complete and remain unmerged Draft work: SEM PR505 head `9478d2ff82335b2822ebc72332ea61a1bb3f555a`; SEO branch `codex/seo-demo-fixture-20260909` head `da783bc295b2a5ca97cdd20ad10bdd58912e207a`; GEO PR504 head `30200cddb044c250ab3bba156660f654bb33999e`. They found that current service entrypoints start background work without a complete demo shutdown gate, and empty credentials/disabled business rows alone are insufficient. Module owners may now implement their own fail-closed demo runtime gates and tests, but still may not connect a database, migrate, seed, merge or deploy.
- The module release branches currently report different Alembic tips (SEM `0088_seo_image_alt_reviews`, SEO `0094_seo_qa_batches`, GEO `0074_geo_ticket_assignment`). A shared demo database must use one coordinator-approved migration lineage after an empty-PostgreSQL online upgrade, repeat no-op upgrade and rebuild rehearsal; module-specific stamping is forbidden. SEM also reports that `sem_tasks` is absent from its current migration head, so demo task rows cannot be loaded until that schema is explicitly resolved.
- SEO completed its draft demo runtime gate at exact `64d63e125777da6cc167b7a21c1d8ed2973d44cb`: demo startup requires `SEO_DEMO_MODE=true`, scheduler disabled and external actions disabled; lifespan/scheduler paths are double-gated and non-whitelisted SEO writes are rejected before routing while login and audited local previews remain available. Full `tests/test_seo*.py` result: 924 passed, 102 skipped. It remains unmerged and undeployed with no database connection or seed, pending split review of fixture/audit versus runtime security changes.
- GEO completed its draft demo runtime gate in PR504 at exact `a14304112442fadf9fcd2a958058b1635b4f4592`: demo startup requires the explicit demo marker plus every scheduler/recovery/worker/patrol/model/generation/publishing/OAuth flag off, Baidu dry-run on and external credentials empty. Lifespan starts no background processes, and the request gate permits only the audited GET/HEAD/OPTIONS tenant and integration reads; legacy GETs, config writes, execution, OAuth callbacks and public shares are rejected. GEO tests: 1125 passed, 47 skipped; CI frontend-build/pytest/backend-tests passed. It remains Draft, unmerged, undeployed and disconnected from any database.
- SEM completed its draft demo runtime gate in PR505 at exact `fd331082c0a77920822d215557f6e238161f8ef3`: demo startup requires scheduler, Baidu client, external actions and write endpoints all explicitly off; central HTTP guards reject writes except login, OAuth callback is separately denied, sync/OAuth/writeback/model entry points fail before transport, database ledger or approval work, and two historical GET side-effect paths degrade to stored reads. Full isolation suite: 2200 passed, 60 skipped; all relevant CI except the unchanged-lockfile OSV job passed. It remains Draft, unmerged, undeployed and disconnected from any database; runtime security and fixture content should be split before merge review.
- The production workbench customer-switch bug is resolved: PR503 exact `3248ba97575f1ad350bcb554ae4a8520243ddcee` merged to `codex/production-sem` as `19dc024a8543b76c70feb17c6b0a6084f01a5c11` and workflow `34298874843` deployed frontend release `/opt/sem-frontend/releases/20260909T012253Z-19dc024a8543`. The full-screen cockpit now provides its own selector over authorized tenants entitled to any of SEM/SEO/GEO; switching clears prior SEO site and evidence context before re-read. Public cockpit and exact asset were 200 after deployment.
- GEO H2 is formally complete: customer review passed, V5/article22 was invalidated by the approved minimal edit to V6/article23, review reset, old channel drafts became unpublishable, regenerated website/WeChat/Zhihu drafts all bind article23, and the final GET-only evidence proved article versions V1–V6 are retained. No publication occurred; H3/H4 remain separate future work.
