# GEO 本地测试实录

日期：2026-09-05。未提交、推送或部署。

本次在 Codex 浏览器打开 127.0.0.1:5276，实际挂载项目 Vue 页面组件，使用新增的
frontend/tests/local-geo-smoke 内存接口环境。测试结束已关闭标签页和临时服务。

## 浏览器实际操作结果

| 操作 | 结果 |
| --- | --- |
| 打开客户 A 信源分析 | 看到 3 条可用样本及对应内容机会，未出现页面错误 |
| 展开机会 | 显示建议行动和快照 #1/#2/#3 |
| 点击快照 #1 | 跳转 prompt_id=2、snapshot_id=1，并打开原文证据弹窗 |
| 检查原文和 URL | script 标签按文本展示；HTTP 链接可见，javascript 地址仅为文本；弹窗布局检查通过 |
| 创建任务 | 跳转任务 100，内存任务数为 1 |
| 编辑行业并保存设定 | 显示 Brief 已保存；重新打开仍为“本地验收行业” |
| 保存后展开任务来源 | 原问题、观察理由、建议和 3 个快照入口仍在 |
| 同题复测 | 跳转到 prompt_id=2 的快照页，显示“清除提问 #2”筛选标记 |
| 重复创建/打开 | 显示“该问题已有任务，已打开”，内存任务数仍为 1 |
| 切换客户 B | 机会和引用统计为空，客户 A 内容未残留在信源清单 |
| 导出机会与证据 | 下载目录生成 geo-source-opportunities-7.csv，解析到 3 行，对应快照 1/2/3、deepseek/doubao/deepseek |

下载事件监听超时，但文件实际已保存；通过本地文件时间及 CSV 内容确认成功，而非仅凭按钮点击判定。
下载文件仅为合成测试数据。没有打开示例外部引用网站。

## 同轮自动化

- Python：506 passed，无跳过。日志 geo-local-acceptance-tests.txt。
- Node：13 项前端检查通过。
- 独立 GEO 前端构建通过。日志 geo-local-acceptance-build.txt；仍有既有大包提示。
- git diff --check 通过。

## 验收范围

此次补齐了实际浏览器上的主页面点击验证，但数据和写入由内存模拟。
重复任务、保存和客户隔离的浏览器结果验证的是页面如何处理接口响应，真实服务器行为由此前接口测试分别覆盖。
模拟环境启动时曾使用错误的测试路由参数 :id，已改为与组件一致的 :taskId 后重新验收；没有因此修改产品路由。

没有运行真实模型采样、内容生成或发布，没有实测真实登录鉴权、PostgreSQL 锁及持久化，
没有在浏览器验证交付报告页面；JSON/Markdown 交付仍由后端接口串联测试覆盖。
编辑器无绑定事实时仍显示准备完成文案，这是已有展示行为，未据此认定生成条件合格或执行生成。

结论：本地模拟页面主链路及自动化检查通过；真实环境的完整发布验收仍未完成。

## 后续异常路径验收

使用相同本地页面和内存接口，增加可控的证据过期、一次保存错误及延迟保存按钮。

| 场景 | 实际结果 |
| --- | --- |
| 列表加载后证据版本变化 | 建任务提示“证据已变化，请刷新”，任务数保持 0；重新读取列表后创建成功 |
| 简报保存一次失败 | 显示错误提示，行业输入保留；再次保存成功，重新打开仍可读取已保存值 |
| 保存尚未返回时切换客户 | 旧任务编辑区隐藏，迟到响应不会恢复旧任务或显示保存成功 |
| 切换客户的顶部残留 | 首次发现旧标题仍显示；修复后重复完整延迟保存流程，标题为空且旧编辑区不再出现 |

修复仅在 GEO 编辑器：客户或任务改变时同步清除任务、标题、正文、简报本地草稿状态及评分，并取消待触发的自动保存。新增实际状态清理函数的回归测试。

本轮 Node 14 项通过，GEO 构建通过（geo-local-negative-build.txt），git diff --check 通过；浏览器无 error/warn 日志。后端未改动，Python 506 项为上一轮结果，本轮未重复运行。
证据冲突通过模拟接口错误测试页面反馈，不代表此次新增了真实 HTTP 409 或数据库验证。真实服务与发布验收限制保持不变。未提交、推送或部署。
