# 演示菜单交付记录 · 2026-09-10

## 目标与范围

用户要求演示账号能看到 SEM、SEO 主要菜单，并有可查看的演示数据与明细。
同域入口 `/workspace/cockpit`；账号 `workbench_test_readonly`，user_id=5，tenant_id=16，role_id=4。
密码、令牌、管理密钥不记录在本文档。

菜单权限可见、页面读取成功、演示数据存在、操作可模拟是四个不同验收项。
不得仅根据授权成功或 HTTP 200 声称全部功能演示完成。

## 平台已执行

通过正式 `/api/v1/roles/4` 管理 API 更新权限，未直接修改数据库表。
更新前 `/users` 校验 role4 仅绑定 user5，客户为16，角色非系统角色。
原六项 view 保留：seo.site、seo.content、geo.content、monitor.dashboard、optimize.keywords、optimize.searchterms。
新增六项 view：seo.assets、seo.dashboard、seo.alerts、seo.keywords、seo.links、seo.competitors。
更新后共12项 view；未授平台管理或 edit 权限。

以普通演示账号登录后验证 `/auth/me` 返回上述12项权限。
SEO `/overview?tenant_id=16&engine=baidu` 返回200及 demo_meta；
`/keywords?tenant_id=16&engine=baidu&page=1&page_size=20` 返回200、4条演示关键词；
`/content-assets?tenant_id=16` 返回4条；`/workbench/sites?tenant_id=16` 返回演示网站1601。
源版本为 tiger_seo_full_domain / 2026.09.10-v1；页面检查为公开网页预采集，内容、发布、排名为演示。

## 各模块边界与待交付

- 工作台统筹：平台菜单授权、普通身份验收、汇总上线状态；不替模块重写业务接口。
- SEM窗口：生产984c5bec的嵌入样例目前接在 cockpit 接口，模块页面仍需适配；先给最小权限建议，再补页面→接口演示映射与必要测试/PR。禁止真实百度调用与回写。
- SEO窗口：生产8e1ebc97已有网站、看板、趋势、关键词、内容、页面诊断及分发核心读取；优先补 alerts、links、competitors。QA与视频作为后续独立范围，不阻塞首批菜单。禁止真实生成、采集、发布。
- GEO窗口：生产82e07fd1独立前端各页使用 geo.content；保留该view即可，不增加geo.assets/geo.diagnosis。通过integration/read补只读演示页面覆盖，旧业务GET保持拦截。

数据库建表、迁移或合成数据落库仍由用户协调数据库负责人。本轮权限使用已有管理API，不需要迁移。
代码提交、合并、实际部署、普通账号页面验收分别记录，开发开始不等于已上线。

## 待补发布记录

SEM/SEO/GEO本轮代码PR、部署SHA与逐页面验收结果：待各窗口回传。

## 17:25 推进记录（Asia/Shanghai）

- SEO #525 / 7f4ae0f3：已审查三文件差异与权限/隔离测试。专项39通过、回归697通过/107跳过，开发反馈CI全绿。已授权SEO窗口核验准确HEAD后按原流程合并部署；实际部署及普通账号验收待回传。
- SEM #526 / c5d06b29：已有三页演示适配，94 Python和2前端测试通过；目标main，仍需分别准备生产前后端推广。要求检查关键词刷新GET路径与客户可读提示文案，待新SHA。
- GEO #527 / 684a774d：82项focused tests通过，但方案把所有演示入口收缩成一个总览，与用户“菜单丰富”目标不符，暂不批准合并。已要求至少总览、问题监测、回答与引用、内容任务四个可切换演示入口，均保持geo.content:view和integration/read。
- 三开发窗口均已收到推进要求；12项只读角色权限不变。本轮额度剩余13%，未触发5%暂停线。

## 17:33 上线核验（Asia/Shanghai）

SEO实际current已为20260910T092707Z-d77143dc485f。以普通演示身份验证alerts、competitors、internal-links、backlinks全部HTTP200且demo_meta.demo=true，分别4条异常、2个竞品、26个内链节点、3条外链。这是接口与实际部署核验，尚不等同浏览器逐交互全验收。SEM第一批提交已更新985653dc，第二批代码正在修改；GEO四入口方案正在修改，未重新批准部署。

## 17:49 发布衔接（Asia/Shanghai）

- SEM 第一批985653dc已修正文案及刷新保护；生产推广后端#529（9c5eb33e）、前端#530（00cc0e81）。已授权开发核对推广差异和准确HEAD检查后，按main→后端部署核验→前端部署核验顺序推进。尚未收到实际部署结果。第二批#528/a033516b覆盖计划、单元、预算、预警、调整记录，暂未批准生产推广或新增权限。
- GEO #527/fa3b3ed已核对为相对最新生产仅6个前端文件，四入口及回答/任务详情方向通过。要求补请求序号防快速切页旧响应覆盖，并校正模块标题；准确HEAD测试/CI通过后已授权既有流程部署，实际部署待回传。
- SEO 本批已归档关闭；API普通身份验收完成，浏览器逐交互仍未验。QA/video不自动扩大范围。
- 本轮快照SEM/GEO均正在执行发布衔接，SEO已完成；无新增人工或数据库需求。角色仍12项view。最近额度剩余12%，未到5%暂停线。

## 17:59 GEO 上线后普通身份核验

开发回传#527部署b984d8a5ff78fe5e114963fde32608a200174f6a（run34462730234），健康及静态资源通过。统筹用普通演示账号正式登录，独立读取integration/read：summary、questions（12）、content-tasks（2）、answers（limit50返回50，未加完整周captured窗口，不作为本周总数）、首条回答详情与首条任务详情均成功；任务含2个版本、3个渠道稿。仅API核验，浏览器菜单/快速切页交互仍未验。未调用业务写接口，凭证未记录。

## 18:13 SEM 首批上线与第二批边界

- #526 main=45ba7556；后端#529实际生产b9e3c7805662453188ba7c65a4351328e5ee835c，run34463052231成功；前端#530生产3ed5c2e9b6948272f54e53a5f0b6b01412dfb831，run34463526409成功（开发回传）。
- 统筹普通账号登录，带tenant_id=16验证dashboard/today、keywords、search-terms均200，关键词6条、搜索词6条，响应含演示元数据；省略必需tenant_id会422，非登录失败。浏览器逐交互仍未验。
- #528第二批a408d805相对main12文件已初审。要求去掉isinstance(ctx,AuthContext)导致无有效ctx仍落真实查询的兼容路径，直接单测传有效身份；准备生产推广草稿PR待最终核对，未授权第二批上线或扩权。
- 额度剩余11%，未达到5%暂停线。无新增人工或数据库需求。

## 本批收尾（用户要求暂不继续验证）

- SEM第二批修正039f9ac5已通过审查及CI。开发回传#528 main=7d6ef50c；#531后端生产7fe82ebec419454b80fe22b22cf3e8c6b1d22012（run34465719699成功）；#532前端生产37cf821980282f3c9369fa6bee7b1201d0fddf46（run34465893572成功），健康及五个页面静态资源核验通过，无迁移或业务执行。
- 统筹通过现有角色API再次确认role4仅user5/tenant16、非系统角色，新增manage.campaigns/manage.adgroups/manage.account/monitor.alerts/verify.adjustments五项view，API读取确认共17项view。无平台管理或edit权限。
- 用户明确“不要验证，先补”，因此未继续第二批普通身份读取及全模块浏览器逐页验收，不声称全交互通过。
- 本轮约定菜单与数据代码已部署、权限已补；SEO QA/video与模拟执行全过程属于后续独立范围，不自动扩展。结束本轮自动监测。

## 20:57 演示首页数据与排列修复

- 用户截图暴露三个实际问题：SEO页面响应不符工作台读取契约；GEO演示账号首页仍读取正式指标，因模拟样本被正确排除而显示不可用/0；线上指标区丢失原型分组并出现内层滚动，无数据依据的固定演示事件与真实状态冲突。
- SEO #534已部署 dcd989c71c5bba20d38020fd410e34e2ca36f2f8（run34479343695），无迁移。普通演示身份的线上响应经现有 createSeoAuthorizedClient/read-only client 重放，contents=4、pages=26，均通过。
- 工作台 PR #533已部署 production-sem=54dfdc542caa368541a15bfdb56a43aa2391a7d5（run34479743912）。精确演示身份先走GEO正式资格预检，再读取demo-summary/capabilities；显示50%提及率、18次提及、12次官网引用、36条回答、3个演示覆盖引擎，全部标记不进入正式指标。恢复“趋势与投入/内容与品牌”分组，去掉指标区限高内滚动和固定伪事件，无真实事件时不留空事件面板。
- 验证：PR #533 pytest、sem-frontend-build、生产发布全部成功；线上入口引用 AcquisitionCockpitView-cxfU9G0a.js，包含新分组与GEO演示消费，不再包含“正在播放演示事件”。未代用户完成浏览器视觉验收。

## 21:45 老虎演示身份与三模块内容统一

- 通过现有客户管理 API 将专用租户16的显示名改为 `TIGER 老虎新材料（演示）`；账号、租户ID、权限及模块开通状态未变。
- 工作台 PR #535 已部署 production-sem=`58b2829b7872b35b5cff7e48ddcd6512a73f4a40`（run34482310666）。演示身份下智能区明确显示“AI 演示助手 / 交互演示台 / 演示模式”，并说明回答和屏幕联动仅用于产品体验；真实客户模式仍保留 DeepSeek 指令能力文案。
- SEM PR #536 已部署 production-sem-backend=`6674930beb1a9915de2b875a4da962d0b56ecca8`（run34482880274）。演示关键词、搜索词、账户、计划、预警与调整记录统一为 TIGER 粉末涂料及表面技术场景；演示ID、指标和只读边界未变。
- GEO PR #537 已部署 production-geo=`2aff472b39ab02c70a903d06547d311310960fc0`（run34483721529）。12个问题、72条回答、事实卡、任务及渠道稿统一为 TIGER 粉末涂料场景；全部继续标记 synthetic/never_official，正式指标仍不采纳这些样本。
- 普通演示身份线上核验：SEM关键词与搜索词、GEO demo-summary 均HTTP 200，均命中 TIGER/老虎内容且不再出现旧设备运维/G-Snipers Demo主题。线上驾驶舱资源包含新的演示助手文案；浏览器视觉仍需用户刷新后查看。

## 22:42 经营全景信息层级恢复

- 用户确认以历史原型V57的信息架构为蓝本，保留当前真实只读数据接入。PR #538 已合并并部署 production-sem=`ae974093e6fb0e1c49f981cb571dfb7323876f1a`（run34491717107）。
- 首屏恢复“正在积累的成果 / 需要推进的事”，其下按“趋势与投入 / 内容与品牌 / 待办与结果”分区；卡片提供“讨论这项 / 展开依据”，对话区与数据区约30/70。
- 摘要只使用当前 cards/state/urgentCount；无固定事件或伪实时。独立审查发现并修复“花费/展现被误称成果”的P2，最终仅显式 `summaryRole=outcome` 的业务结果进入成果摘要。
- CI 的 pytest、sem-frontend-build、Deploy SEM frontend only 全部成功；线上资源 `AcquisitionCockpitView-D4a4A8j9.js` 已包含新双栏、三个分区和卡片动作，旧“实时战况”文案已移除。

## 13:32 经营全景探索与范围比较上线

- 工作台 PR #539 已合并并部署 production-sem=`4c0a06a878545d9fc330387a25bb314e85629355`（run34566044910）。
- 数据卡支持查看路径前进/后退；切换模块、时间范围、指标与 SEO 网站都会记录路径并可恢复。AI 屏幕指令也进入同一历史。
- 支持保存当前范围，并对同一指标比较两个时间范围；SEO 比较严格限制同一网站，SEM/GEO 不受网站维度影响。百分比差值显示为百分点，底层数值 delta 保持数值型。
- 原“展开依据”改为“独立查看”，继续复用只读证据弹层，不触发业务动作。
- 独立复审精确 HEAD `63e90c675efcc5c2a97d41e280cbfd4b9b0d9690` 通过，未发现新 P1/P2；探索状态测试 6/6、既有驾驶舱回归 6/6、生产构建和 CI 全部通过。
- 线上资源 `AcquisitionCockpitView-3-4si20Q.js` 已核对包含“当前查看路径 / 保存当前范围 / 与保存范围比较 / 独立查看 / 百分点”。未代用户做浏览器视觉验收。

## 14:08 SEM / SEO 真实数据图形上线

- 工作台 PR #540 已合并并部署 production-sem=`81b93204a2990a222b8a9dc9f36732f7e5c35075`（run34568197533）。
- SEM 指标卡增加真实日趋势；缺报保留为 `null` 并在缺口处断线。曝光卡增加严格的“曝光→点击”两阶段漏斗：仅在所选窗口无缺报、曝光/点击关系及服务端 CTR 一致时展示；异常、CTR 缺失或曝光为0时封闭隐藏或不计算，不扩展为电话/咨询/线索。
- SEO 内容卡增加“当前内容状态分布/库存”，直接使用 `contents.total/status_counts`；总数无法对账时不绘图，固定展示六个已知状态并保留未知状态，不推断转化率、处理耗时或页面检查通过。
- 图形沿用卡片焦点、探索历史、独立查看和讨论；整图单一键盘入口，支持方向/Home/End/Enter/Space；趋势采用整图 scrubber，触屏保留纵向滚动；零值条宽严格为0，SEO状态行触控高度至少44px。compact 模式隐藏图形。
- 首轮独立审查发现 click>impression、零值假条、密集趋势命中区三个 P2，均在 `b04803334e7b2cca80e4b19b8adb9c16e543a5ae` 修复并通过复审。随后 CI 暴露旧 SFC harness 不支持本地组件 import，测试工具在 `e17074eee66bbc308db980d79338908129224479` 修复且未绕过断言。
- CI pytest、sem-frontend-build、Deploy SEM frontend only 全部成功；线上资源 `AcquisitionCockpitView-DM830iRj.js` 已核对包含“曝光到点击 / 折线在缺口处断开 / 数据关系无法核验 / 当前内容状态分布直接来自状态库存”。未代用户做登录后的视觉验收。

## 15:10 开发重心调整

- 用户要求暂停获客工作台数据大屏与对话智能体的新增开发；当前线上版本保持不变，不回滚已上线能力。
- 开发资源转回 SEM、SEO、GEO 三个模块自身：SEM 收口待调整→审批→受控执行→对账→行动台账；SEO 建立客户执行→系统复核队列；GEO 补调度可观测与 H3/H4 只读验收摘要。
- 统一减少检查步骤：每批只做一次范围核对、相关测试、合并前一次独立审查、部署后健康检查；不重复全库审计，不因真人测试阻塞其余开发。
- 当前不需要数据库协助。老虎真实回写、真实平台账号或真实发布进入必要阶段时，再向用户提交一次性、可执行的真人需求。

## 17:49 三模块开发批次收口（2026-09-11）

- 本批暂停工作台数据大屏和对话智能体新增开发，只推进三个业务模块；检查固定为范围核对、相关测试、一次独立复审和一次部署后健康检查。
- SEM：PR #542 已合并，main=`d0ef4953ff3efcd117450e5468cf293a9cbeeae6`；生产后端=`9bd9450205cab23c8880667dfbe60bb7ba260669`（run `34584947195`），生产前端=`380f8947ed49fb34d2b98d7c279b6dc4e1138ad0`（run `34585130461`）。已统一待调整、审批、受控执行、结果读取、检查依据和行动台账，并修复执行入口权限与账户动作范围文案。健康、构建与部署均通过，未触发真实回写。
- GEO：PR #543 已合并，生产=`0f6156a53112a196cb3ce3ad1330cbca8342d9e0`（run `34585923891`）。新增调度运行状态和内容任务 H3/H4 只读验收摘要；修复失败批次被覆盖、旧证据误判健康和启动失败状态悬空。`/geo-health` 为 200，新接口已挂载且鉴权生效；未触发发布、抓取或重试。
- SEO：PR #541 的准确复审 HEAD=`9ebc9f4e88e9bdd4c2ae2ab9362969c30768386f`，独立复审通过并部署 production-seo=`f0b5d41b4a984230d6b6dea23b2c9da0183bc8ef`（后端 run `34586358023`、前端 run `34586357977`、基线 run `34586358421`，均成功）。客户执行到系统复核队列已覆盖图片、发布地址、页面复查与外链检查；未核实/不可用图片仅向具备 `seo.site` 编辑权限的人员提供重试动作，只读客户不暴露执行 URL。`/seo-health` 为 200，新队列生产路由已挂载且鉴权生效；未触发发布、抓取、图片重试或生成。
- 当前无数据库需求。SEM 老虎真实回写、SEO 真实平台动作和 GEO H3/H4 真人流程均可在各自必要阶段单独验收，不阻塞其余代码开发。

## 19:30 SEM 客户执行权限自助管理上线（2026-09-11）

- 用户要求超级管理员可直接为客户配置 SEM 真实回写权限，后续日常开通无需再由工作台统筹逐次介入；本批不扩展 SEO/GEO 执行权限，也不恢复数据大屏或对话智能体开发。
- 功能 PR #546 已合并，main=`00fd16b37dec166880d17edea439a0fbfb99d074`，最终审查 HEAD=`8a430ad44a6b67800f2d297f161925cdc208af38`。独立复审发现并修复旧环境授权被界面误称为“默认演练”的问题，最终无 P1/P2。
- 超级管理员可在统一客户管理页按客户和百度账户配置允许的动作、每日真实执行上限及单次出价变动上限，也可一键暂停；每次保存要求填写原因并记录操作人、时间、版本和历史。策略写入既有 `tenant_modules.module_settings.sem_live_write_policy`，未新增表或迁移。
- SEM 回写链路以数据库策略为账户级授权依据，继续保留审批、指纹、锁、结果对账和 Baidu 客户端二次门禁；服务器全局演练/应急开关仍是最高安全闸，管理员页面不能绕过。兼容期旧环境授权只在首次保存数据库策略前生效，并在界面明确提示来源。
- 后端 production-sem-backend=`5f37e133416a0bc18abbd1a08087fc2333d2dd7d`（run `34592856564`）已部署，`/health` 为 200、`db=ok`，新管理接口未登录返回 401；无迁移、无客户策略写入、无真实百度操作。
- 前端 production-sem 功能版本=`f3be70f855221245379af6324070b95f31f53407`（CI/前端 run `34593573264` 成功）。统一客户管理页已包含“执行权限”、账户选择、动作范围、额度、确认提示、历史和暂停入口。
- 首次 `/platform/*` 路由部署实际已应用候选配置，但烟雾检查只接受相对 `Location`，将 Nginx 返回的同源绝对跳转误判后自动回滚。PR #550 修复为仅接受精确相对路径或精确同源绝对地址，production-sem 最终=`eb4610c3faccac3c61f60d79424a8c3bfa493de1`；受限部署模块 SHA-256=`05a893b64286ba91ce3792ed4749908b59672e2bea9431f8f048a5fa01894e46`，路由 run `34595140302` attempt 2 成功。安装和部署未改数据库、客户策略或业务数据，开发按用户要求暂停。

## 00:30 三模块非真人收口批次（2026-09-12）

- 用户恢复三个业务模块开发，工作台数据大屏和对话智能体继续暂缓；本批不等待真人测试，只处理各模块可独立完成的最高优先级缺口。
- SEM：功能 PR #553、前端生产 PR #554、后端生产 PR #555 已合并。生产前端=`4ccea39296860b6ce9d8f7b3b2b77bf3b8e65889`（run `34621460527` 成功），后端=`4f3b389dcb47c420edcaaf51cd2268b1f540364e`（run `34622091261` 成功，`/health` 返回该 release、`db=ok`）。单关键词调价提交前即时只读核验客户/账户、演练或真实候选、授权依据、额度及单次上限；预检异常或进行中撤权均失败关闭；结果展示行动台账和资金确认编号。未关闭全局演练总闸，未触发百度写入。
- SEO：PR #552 已合并并部署 production-seo=`ad50943b36f07654629b081b0e8729d1ec2c0546`（run `34620076246` 成功）。图片核验重试保留最近20次结果摘要、重试发起人和UTC时间，新结果继承历史，不再覆盖旧证据；权限、5分钟冷却和抓取逻辑未改。`/seo-health` 为200、`db=ok`、schema=0098；未触发抓取或发布。
- GEO：PR #551 已合并并部署 production-geo=`ece4bf92ab87b1e406076d3daeab06b93679b47d`（run `34619881626` 成功）。H3系统前置未满足时H4强制阻塞，返回受控阻塞原因，修复重复发布登记及审核未通过时的错误就绪，并停止回传旧监测错误原文。`/geo-health` 为200、`db=ok`；未触发采集、生成或发布。
- 本批无迁移、无数据库请求、无客户授权变更。剩余真人边界：SEM小流量真实回写、SEO真实平台发布、GEO H3/H4真实渠道与缓存行为；三者互不阻塞后续非真人开发。

## 01:05 三模块第二批收口（2026-09-12）

- 继续遵守“工作台大屏与对话智能体暂缓、只推进业务模块”的范围；本批只做相关测试、一次独立复审、CI 与部署后健康检查，未做全库审计。
- SEM：功能 PR #558、前端生产 PR #559、后端生产 PR #560 已合并。生产前端=`1a96a647d4cfa48db3c0a7c297ea175052117416`（run `34624613662` 成功），后端=`f2705fd11a87373baffcfb5e950bd32c932579f3`（run `34624699036` 成功，`/health` 返回该 release、`db=ok`）。单关键词修改匹配方式及暂停/启用在确认前读取精确客户、账户和动作策略；提交时服务端再次核验数据库授权、额度、账户状态和资产归属；结果返回行动台账、演练/真实结果及待对账状态。批量流程未改，未触发真实百度写入。
- SEO：PR #557 已合并并部署 production-seo=`a83e2494df2089e2ed73c227f05f81aa24937a87`（后端 run `34623314331`、前端 run `34623314276`、基线 run `34623316002` 均成功）。系统复核队列增加最近一次发布尝试的动作、状态、发起人和起止时间；不返回请求正文、响应正文或原始错误。`/seo-health` 返回 `db=ok`；未触发发布或抓取。
- GEO：PR #556 已合并并部署 production-geo=`37eb6c976b0ed2c3bd711653c2c255ceb0d06aed`（run `34623178266`、基线 run `34623178296` 均成功）。现有上线检查区增加 H3/H4 中文状态、阻塞原因及后端要求说明；只读摘要加载失败不影响原检查区，未增加任何执行按钮。`/geo-health` 返回 `db=ok`；未触发采集、生成、重试或发布。
- 当前无数据库、管理员或真人协助需求。真人验证仍仅在 SEM 老虎小流量真实回写、SEO 真实平台发布、GEO H3/H4 真实渠道阶段按需提出，不阻塞下一批非真人开发。
