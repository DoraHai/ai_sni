# H1-复验-2 留档与开发侧核对

测试人员报告：2026-09-06 15:16 Asia/Shanghai；Chrome 151 / Linux x86_64；客户诺德；任务14。页面报告 V1 保留、1397字；本次更新母稿未成功产生新稿。未审核、未发布、未操作渠道；任务未清理。渠道稿/发布记录均无。

执行步骤：硬刷新 → 刷新检查 → 保持原3条事实与创作要求（场景、观点解读、预约诊断/咨询选型）→ 更新母稿 → 拦截后停止，无再次尝试。

评分报告：结构、可摘取、来源可信、品牌推荐100；事实支撑25；问题覆盖80；对比覆盖85。绑定资料3/3，无排除项；新稿未出，生成日期未验收。旧稿仍有设备、行业和机理的证据缺口以及2025-04-11更新时间，不把旧稿问题当本次新稿结果。

报错原文：

> 母稿存在事实卡未能支撑的表述，已拦截：案例表述「成功案例」

结论保持：**H1受阻，门禁触发有效但非完整通过；H2/H3/H4继续受阻。** “能否重复”保持未复测，不补写成可重复。页面的 V1 标签并不提供实际 article_id/version_no，本轮未读取或重写数据库版本，不擅自填入新生成版本ID。

## 开发侧只读核对

1. 独立的15:25部署核对确认当时线上为d100907；本次测试报告本身未记录commit。本地 Brief/版本展示/跨语言/只读接口改动尚未部署，不能认为这次已经验收了它们。
2. claim_guard.py 的案例规则是关键词判断：正文包含成功案例且事实文本不含该词就命中；输出 token 与 excerpt 均仅为关键词，format_ungrounded 不提供完整语境。
3. 本地离线合成复现（无数据库、无模型调用）：
   - “暂无可核验的成功案例。” → case命中。
   - “请提供可核验的成功案例资料。” → case命中。
   - “该产品已有成功案例。” → case命中。
   前两项说明现有规则确实存在把证据缺失说明、资料请求当成案例断言的误拦风险。第三项仍应在无证据时阻断。
4. **无法据此判定这次真实生成是误拦。** 报告只有“成功案例”四字，没有被拦完整句子；不得猜测失败草稿内容，也不得因为可能误报就放宽真实案例断言门禁。

下一修复包应保留完整命中句及原因，区分缺失说明/资料请求与实际案例断言；覆盖混合句（例如“暂无资料，但已有某客户成功案例”）及否定/条件被删除的负例，不能按含“暂无/请提供”跳过整段。本次只记录发现和方案，未继续改业务代码。

测试人员当前无需再点击生成，不清理任务14，不推进H2/H3/H4。保留原稿对照；下一次复验应明确新部署commit并同时记录article_id、version_no、来源，安全拦截仍不作为完整H1通过。

## 后续本地修复（用户指示“下一步”后）

- 案例检查改为逐句/逐分句判断，仅对严格匹配的“暂无可核验案例”“请提供案例资料”等资料说明或请求放行；不因整句包含“暂无/请”就跳过后面的实际断言。
- “暂无资料，但已有某客户成功案例”、双重否定、条件引导后的案例断言仍会阻断。
- 事实标题或另一主体的事实中仅出现“成功案例”不再放行正文；目前仅完整事实原句复述可直接支持，其他改写仍需核验，未实现通用语义证明。
- 案例命中保留完整原句，生成错误和 lint 展示完整 excerpt；同词在不同句出现分别记录，超出显示条数时明确提示剩余项。
- 新增 `tests/test_geo_case_context.py`，含合成语句、资料请求/混合断言、主体/否定/条件、长句错误、数字门禁独立性和模拟生成链路测试。模拟模型不访问真实供应商，不生成客户内容。
- 全量 GEO 回归：**905 passed、17 skipped**；diff 检查通过。此处统计范围为 tests/test_geo*.py，与此前包含其他模块的927项统计范围不同。

本轮未提交/部署，未重新生成、审核、发布或清理任务14。H1受阻结论不变；部署后再使用新版本复验，不能以合成样例通过替代真人出稿验收。
