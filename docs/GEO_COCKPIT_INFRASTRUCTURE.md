# GEO 经营驾驶舱基础设施

本实现只提供模块接口，不连接驾驶舱、不发送外部业务请求。

## 共享字段：保持用户最新契约
Task 返回字段严格为 id、module、action_type、title、params、status、created_by、assignee_role、completion_evidence、created_at、updated_at。
Metric 返回字段严格为 metric_key、value、unit、as_of、trend_7d。

trend_7d 为 null 或以下对象：
```json
{"direction":"up","change_pct":50.0,"change_abs":2}
```
direction 为 up/down/flat/null，仅事实方向，不评价好坏。change_pct=(本期值−前期值)/abs(前期值)×100，change_abs=本期值−前期值。可算时都填；前期为0时 change_pct=null，绝对变化及方向保留；无合格前期数据时整个对象为null，不能用0冒充未知。问题/引擎样本集合不一致时也不形成趋势。

按用户确认，周窗口采用 Asia/Shanghai 最近一个完整自然周 [周一00:00, 下周一00:00)，as_of 为窗口结束时刻。trend_7d 比较前移7天的完整周。日期参数 week_end 必须是周一且不晚于本周周一；窗口参数是查询参数，不增加指标对象字段。
用户同步的 SEO 图片口径同时留档：approved 只表示草稿审核通过，须进入待核实，复用 seo_crawler + save_page_snapshot 定向重抓确认实际修复后才计入完成。本轮没有修改 SEO。

## 接口
路径均以 /api/v1/geo/integration 开头，使用现有认证，tenant_id 必传。读取需要 geo.content 查看权限，写入需要编辑权限，跨租户拒绝。

| 方法 | 路径 | 含义 |
|---|---|---|
| GET | /metrics/snapshot?tenant_id=7&week_end=2026-08-31 | 五字段指标列表；纯读取，不重建缓存、不提交事务 |
| GET | /metrics/dictionary?tenant_id=7&week_end=2026-08-31 | metric_key 到一句话口径文档的映射，含同期竞品名称 |
| POST | /tasks?tenant_id=7 | 创建统一任务，返回201 |
| GET | /tasks?tenant_id=7 | 查询；支持 status、after_id、limit（1–200） |
| GET | /tasks/{id}?tenant_id=7 | 读取任务和完成证据 |
| PATCH | /tasks/{id}?tenant_id=7 | 提交目标状态，done 由服务端验证真实指标 |
| POST | /tasks/{id}/baseline?tenant_id=7 | 仅在原基线缺数据时补采基线；有效基线不可覆盖 |

响应模型已进入 OpenAPI，可从现有 /openapi.json 查看 MetricSnapshot、MetricTrend、TaskContract。

创建示例：
```json
{
  "module":"geo",
  "action_type":"improve_content",
  "title":"补齐选型问题的品牌事实与来源",
  "params":{"metric_key":"geo.visibility.ai_mention_count_7d","direction":"increase","min_delta":2},
  "status":"open",
  "assignee_role":"geo_operator"
}
```
created_by 从认证身份推导，用户为真实user_id，服务API密钥为cockpit；提交不同身份返回403。params.metric_key 默认提及次数；direction 为 increase/decrease，默认 increase；min_delta 默认为0，但完成仍须严格正向变化，不能零变化完成。assignee_role 是执行角色标签，不授予权限。

## 指标字典与可比性
- geo.visibility.ai_mention_count_7d：合格回答中 mentions_brand=true 的条数，一条回答最多计1次，unit=count。
- geo.visibility.ai_mention_rate_7d：品牌提及回答数/全部合格回答数×100，unit=percent。
- geo.visibility.ai_visibility_score：50×品牌提及率+50×自有域引用率（两率为0–1），unit=score，范围0–100。引用分母为全部合格回答，一条回答有多个自有域链接仍计1次；自有域来自启用的 website/docs 渠道及其子域；没有配置时综合分为null。该分数不是网站诊断分或正文质量分。
- geo.competitor.{名称哈希}_mention_count_7d：同一批回答中该竞品出现的回答条数，unit=count。名称strip/casefold后SHA256前20位作为稳定key，单条回答去重。字典返回原归一名称。任务基线已记录的竞品会继续保留，即使近两周没有命中也能显示已观测到的0。

合格集合：同一租户、非品牌点名问题、真实v2 API、判读完成且引用未标为错误；还必须匹配已完成服务端巡检保存的原始结果（原文、问题、引擎、提及、竞品、引用及巡检时间范围）。手工贴入的“API采样”标签不足以进入驾驶舱统计。未被服务端巡检追溯的单次手工确认探测也暂不纳入。
每周至少8条、3个问题、2个引擎，否则各指标value=null；无样本不伪造0。上述数值是有限采样结果，不是全网统计，也不证明任务造成因果改善。名称别名除大小写与首尾空格外不自动合并。

## 任务如何复用原工单
复用 geo_action_tickets，无新表、无结构迁移。advice_code=cockpit:v1:task，progress_first 保存契约元数据，baseline_snapshot 保存服务端基线，progress 保存服务端完成证据。
状态映射：todo→open，doing→in_progress，done→done，cancelled→cancelled。该适配入口只管理通过统一接口创建的工单；历史人工验收工单不伪装成具备真实指标证据的任务。原有工作队列和诊断工单保留原流程。

完成条件：
1. 同一任务加行锁，完成/取消为终态，重复提交同状态幂等返回。
2. 后测是任务创建之后的完整自然周，且晚于基线。
3. 指标前后均非null，问题×引擎集合、每个组合的采样次数及自有域配置一致，避免增加采样量或改变样本权重被当成任务效果。
4. 达到指定方向和最小变化量。
5. 服务端生成包含前后指标、delta、快照ID、核验时间和带租户/周参数的指标来源地址；客户端不能提交 completion_evidence。
旧 action-tickets 创建/人工验收入口拒绝冒充或改写这些统一任务。没有合格数据的任务保持未完成，可补采一次有效基线后继续。

## 审查问题修复
- 竞品对比在聚合前过滤不合格样本及品牌点名题，样本不足不再输出胜负或竞品百分比。
- 日聚合不再截断15个竞品；竞品日接口从合格原始快照只读重算，绕开旧缓存截断。
- 网站抓取连接绑定已校验公网IP，保留原Host和TLS SNI，禁用环境代理和跨请求连接复用，重定向逐跳验证。
- robots 区分整站拦截、局部限制及特定UA空规则；保留局部限制统计，不将其算整站拦截。
- 索引判断汇总robots meta，识别none及X-Robots-Tag。
- 报告编辑撤销旧确认；完整版本证据保存在既有JSON字段，恢复文本和证据一起恢复为草稿。旧版本没有完整证据时拒绝恢复，避免拼接最新证据。

robots标准参考：https://www.rfc-editor.org/rfc/rfc9309.html 。索引指令参考：https://developers.google.com/search/docs/crawling-indexing/robots-meta-tag 。固定IP同时保留SNI的实现依据：https://www.encode.io/httpcore/extensions/#sni_hostname 。

## 验证边界
有模拟会话的路由/锁顺序测试、HTTP契约与权限测试、样本来源一致性测试、DNS固定和重定向阻断测试；实际HTTPS抓取example.com成功。未进行内网攻击或生产写入验收。指标读取不会自动重算或写入缓存。

最终本地验收：626项后端测试、62项前端测试通过，GEO独立生产构建与git diff --check通过；测试没有调用真实AI模型或写入生产业务数据。

## 二次审查修复
- direction 传入数组或对象时返回参数校验错误，避免触发500；过早的week_end在查询前拒绝，避免日期运算下溢。
- 完成证据增加前后各问题×引擎采样次数，要求分布一致；旧基线缺少该记录时拒绝完成，需新建任务采集可验证基线。
- 指标快照仍按实际回答条数统计；上述采样分布约束用于任务验收，不改变共享指标公式或trend_7d格式。
二次审查验收：637项后端测试通过，git diff --check通过；本轮未修改前端及SEM/SEO代码。


## 精确复测与发布核验（2026-09-05）

共享任务11字段、指标5字段保持不变。辅助执行接口位于同一 `/api/v1/geo/integration` 前缀，均要求认证及 tenant_id：

- `GET /tasks/{id}/execution-readiness`：只读返回基线阻塞原因、可复测计划、发布账号能力、已核实发布证据和完成证据。不会创建默认渠道或写缓存。
- `GET /tasks/{id}/retest-plan`：从已冻结有效基线复制问题原文、每个问题×引擎的实际次数；不启动AI调用。
- `POST /tasks/{id}/retest`：202返回run_id。必须处于基线结束、任务创建及关联内容首次发布核验之后的完整上海自然周；当周已有样本、在途任务或复测预约时拒绝追加。重复同任务同周请求返回原run_id，不产生新调用。
- `POST /tasks/{id}/publication-check`，正文 `{"publication_id":123}`：重新安全抓取当前版本的真实发布URL，检查标题与至少3段、80%以上的正文片段；保存内容哈希、发布/版本ID及首次和最近核验时间。仅有published标记、标题或隐藏正文不通过。失败不生成完成证据。

创建内容优化任务时，params.content_task_id 可绑定本租户内容任务。完成时重新核验发布页，后测整周必须晚于首次核验；正文版本或URL变化会重新开始核验时钟。HTML匹配并不证明浏览器渲染、搜索收录或AI已经引用。

精确复测最多200个单元，不填充缺失组合、不在失败后偷偷加样本、不回退模拟引擎。失败预约保留，本周不会自动重启或补齐从而偏移样本权重；下个满足条件的自然周可再次启动。执行结果列出缺失单元，未持久化/判读未完成的回答不能计入合格复测。

趋势新增可比性限制：问题原文、问题×引擎及各组合的样本次数须一致，否则trend_7d=null。完成时还重新读取基线来源，基线快照ID、分布、问题、域名或指标被更正时拒绝沿用旧证据。历史基线缺少问题原文不能推测补写，需新建任务采集可追溯基线。

本轮验收：668项本地后端测试、62项前端测试通过；当前代码在真实PostgreSQL独立临时schema完成3项并发终态测试，测试schema已清理。未修改SEM/SEO代码或运行数据库迁移。详细范围、修复和剩余限制见 GEO_DETAILED_REVIEW_20260905.md。


## 模型来源一致性

内部周快照增加model_counts，按问题、引擎、供应商、模型记录实际样本次数；来自原始服务端巡检cell，不接受用户填写。对外五字段和数值公式不变。模型分布变化或来源缺失时趋势返回null，统一任务拒绝完成；完成证据携带前后模型分布，完成时重读基线也校验此字段。

精确复测要求每个问题×引擎只有一个可追溯模型版本，且次数与基线矩阵一致。调用前比较实际解析的模型/供应商配置；不一致的单元不发起模型调用。旧有效基线缺少model_counts不能推断补写，应保留旧记录并新建可追溯任务。供应商未公开的同名模型内部更新无法由此识别，仍不应把观察变化当成内容因果。

本轮本地回归672项通过，真实数据库专项另执行。无前端、数据库结构、SEM或SEO代码改动。


## 运营执行面板

验收工单页新增“指标验收任务”，从统一任务列表分页读取，展示基线、真实发布、精确复测和实际指标验收。通过原有baseline/publication-check/retest/PATCH接口执行；浏览器不写完成证据或直接变更终态。原诊断工单列表排除统一任务，避免误走旧验收入口。

execution-readiness辅助响应增加baseline_valid、baseline、can_retest、retest_blocker、latest_retest、publication_candidates。can_retest额外验证当前完整周窗口、发布证据与采样预约。发布候选仅来自本租户关联内容任务的最新正文版本、published且有URL的最近50条记录；真实核验仍会重新抓取，不把候选当成已核实证据。终态按钮停用，失败时显示服务端原因，复测受理不表示任务完成。

面板切换客户/任务或卸载后忽略旧响应，重复点击只发一次，支持200条分页；只读刷新不调用采样和发布。共享任务11字段及指标5字段保持不变。

本轮692项后端、72项前端通过，独立GEO构建成功。真实浏览器使用本地内存API完成选择任务、采集基线、选择发布记录并核验、复测受理、拒绝提前完成、切换客户清空的测试；没有触发真实AI、生产发布或客户数据写入。


### 2026-09-06：从文章建立指标验收任务

文章工作台“更多 → 建立指标验收任务”可关联当前文章，选择提及次数、提及率或可见度分数，设置提升量及负责人角色。提及率的提升量使用百分点；目标仍为客户完整周统计，不能解读为单篇文章的独立因果贡献。无合格周数据可先建任务，随后采集基线，不自动记零。

POST /integration/tasks 对 content_task_id 校验当前客户归属并拒绝归档内容。创建时锁定关联文章行；同一文章、相同指标的活动任务如请求内容和认证创建者完全一致，返回原任务（保持现有 201 响应及十一字段契约）；目标或其他字段冲突返回 409 并指明已有任务编号。已完成或取消的任务不阻止后续新任务。未关联文章的原有创建行为不变。

本地验证：后端 697 passed、3 PostgreSQL 专项 skipped；前端 75 passed；GEO 独立构建通过。浏览器使用仅内存 mock 验证实际创建组件的表单和成功反馈，未产生生产业务数据。本轮未执行真实 PostgreSQL 并发创建测试，不将 mock 行锁断言视为数据库并发证明。


2026-09-06 复查补充：提及率及可见度分数目标的 min_delta 上限为 100（绝对变化量），计数指标不受此上限限制。并发创建已通过真实 PostgreSQL 的相同请求与冲突目标两种测试；连同已有完成状态测试，5 项专项全部通过。详见代码审查记录。


### 2026-09-06：验收任务直达与编号查找

创建成功链接携带 evidence_task_id 和 evidence_tenant_id，验收页按当前认证客户查询指定任务及执行条件，不受首屏 200 条限制，也不把额外读取的任务插入分页列表改变游标。页面提供编号查找，可用于继续处理创建冲突提示中的已有任务。跨客户链接只提示切换客户，不自动切换、不读取另一客户详情；错误、无效编号、已删除任务不回退选择其他任务。同编号再次查询可刷新状态。

验证：82 项 GEO 前端测试通过，独立构建通过；本地浏览器验证创建后直达及切换客户清除详情。仅前端变更，无后端契约变更，无生产业务写入。


### 2026-09-06：验收任务状态管理

页面支持 open → in_progress 的开始处理，以及活动任务取消。使用原有 PATCH 契约，取消前在当前任务内确认，明确终态不可重开、已启动采样不会停止。终态禁止新核验或复测。状态变更响应先更新页面，再读取执行详情；详情刷新失败会说明操作已受理并保留已确认状态，避免误判操作失败。确认状态绑定客户及任务，切换后清除。

89 项前端测试通过，独立构建通过；本地浏览器以独立响应对象验证创建、开始、取消及终态操作禁用。无后端变更，无生产任务状态修改。


### 2026-09-06：任务状态筛选

验收任务列表支持全部、待处理、进行中、已完成、已取消，复用后台 status 过滤并在翻页时保持条件。开始/取消后不再匹配的任务从列表移除，详情保留并提示不属于当前筛选。分页游标独立记录服务端返回的最后 ID，不随列表移除行回退。91 项前端测试、独立构建及本地浏览器筛选验证通过。无后端契约变更。


### 2026-09-06：执行阻塞总览

验收页增加当前筛选下已加载任务的执行条件总览，每批最多20项、最多3个并发只读请求。显示缺基线、发布待核实、缺发布证据、复测执行中、可启动复测、复测异常或等待后测/验收。请求失败明确为条件未知，不当作无工作；复测完成不当作指标达标。终态直接依据任务状态展示。总览条目进入原有任务详情操作，不自动发布、采样或验收。切换客户/批次后丢弃旧响应并停止调度后续旧请求。

验证：97 项 GEO 前端测试通过，独立构建通过；本地浏览器验证总览进入任务，以及采集基线后由缺基线转为发布待核实。无后端契约、数据库或其他模块变更。


### 2026-09-06：复测结果诊断

任务详情展示服务端 retest_result 的 comparable 判断和 missing 单元，列出问题、引擎、缺少合格样本次数，并提供问题采样记录入口。采样结束不代表矩阵匹配，矩阵匹配不代表完整周指标达标；总数一致但分布不同仍显示不匹配。缺少结果显示未知，失败保留错误信息。总览在无可启动复测条件时提示复测样本不匹配，不自动补采样改变统计权重。

107 项前端测试和独立构建通过；本地浏览器验证 10 次计划、8 次合格及 DeepSeek 问题 71 缺少 2 次的展示。无后端或其他模块改动，无真实采样调用。


### 2026-09-06：真实执行发现的基线窗口误导修复

新增 GET /integration/tasks/{task_id}/baseline-readiness，按认证客户及任务目标指标返回最近完整周与当前周的合格样本/题目/引擎覆盖、周边界和诊断状态。仅在任务详情点击检查时读取，避免总览逐任务重复聚合。复用原始巡检来源校验及指标资格规则；不返回当前周指标值、不保存或提前冻结基线。正式五字段指标接口继续拒绝未结束周。

基线采集无有效完整周时，409 提示区分本周样本已足但等待周结束、采样不足、自有域缺失；不再一律要求继续采样。样本覆盖不保证将来的模型一致性或最终达标，周结束后仍需校验。

本地后端703项通过（5项数据库专项本地跳过，另在真实PostgreSQL隔离schema全部通过），前端107项通过、独立构建通过。真实数据诊断在只读事务执行；本地浏览器验证显示样本覆盖与周边界。无数据库迁移，无SEM/SEO修改。


## 发布结果核对与恢复（2026-09-06）

- `GET /api/v1/geo/content-tasks/{task_id}/deliveries?tenant_id=...`：读取该客户任务的发送状态、账号、模式、时间及人工核对历史，不返回连接器原始响应或凭据。
- `POST /api/v1/geo/content-tasks/{task_id}/deliveries/{variant_id}/{delivery_id}/resolve`：请求包含 `tenant_id`、`action`、10–1000 字 `note`；操作要求已登录的人员身份、GEO 内容编辑权限，以及当前稿件通过人工审批。
- `action=confirm_published`：同时提供 `published_url`（https）。仅用于发布操作；复用防 SSRF 抓取和正文匹配机制，匹配当前稿件后补齐发布记录、保存正文哈希和核验时间。不直接完成指标任务。
- `action=allow_retry`：必须提供 `confirmed_not_published=true`，由操作人员核对渠道后台确认没有对应文章或草稿。只解除重试限制，不执行发送，也不作为指标改善证据。
- `sending` 记录至少等待 10 分钟后才能人工核对；无法解析发送时间、稿件版本变化或记录已经成功时拒绝恢复。历史版本记录需要人工排查，不能拿新正文为旧发送核销。
- 发送与恢复共用任务行锁。每次发送预约具有独立标识，恢复后旧工作进程不能继续发送。重试保留已有核对历史，单条发送记录最多 100 次恢复操作。
- 分发页面提供核对入口；运营异常提示仍链接到此页面。此功能不改变驾驶舱 Task / Metric 契约，不影响 SEM/SEO。

真实 PostgreSQL 测试使用 `GEO_TEST_POSTGRES_URL` 显式启用。测试在随机独立 schema 复制所需表的空结构，不复制客户数据、不使用生产序列；模拟外部连接器，结束后删除测试 schema。覆盖同账号并发防重、不同账号记录保留、读超时禁止重发，以及发送和恢复并发互斥。


## 自动发布门禁：全量回归与真实数据库并发

统一入口为 `python ops/run_geo_checks.py`，自动发现 `tests/test_geo*.py`，同时运行指标、Markdown 表格和文章质量测试。入口固定使用测试配置、忽略本地 `.env`、清空继承的 API Key，并使普通测试数据库不可连接。普通模式跳过明确要求 PostgreSQL 的测试。

GEO 检查与部署前验证均运行 `python ops/run_geo_checks.py --postgres`，配有独立 PostgreSQL 16 临时服务。仅接受回环地址、`geo_ci` 数据库和 `geo_ci` 用户；初始化前必须没有已有表，只创建七张测试所需的空表结构。连接配置缺失会退出失败，不能把数据库测试跳过后当成验证通过。生产部署依赖此验证任务成功，不使用生产数据库凭据，也不执行迁移。

真实数据库部分包含四项发布/恢复并发测试和五项统一任务创建/完成并发测试。测试失败时取消尚未结束的协程，使连接释放和临时 schema 清理能够执行。此次本地统一入口 768 项通过，9 项数据库测试另行在真实 PostgreSQL 隔离 schema 中通过；新增工作流需推送后由 GitHub Actions 实际运行。


## 恢复待办可操作性

发送记录列表增加 `can_confirm_published`、`can_allow_retry`、`blocked_reason`、`available_at`，顶层增加 `actionable_count`、`blocked_count`。这些是 GEO 分发专用接口字段，Task / Metric 共享契约不变。可操作性与执行接口共用恢复规则；列表结果仅是当前快照，实际提交仍在获取任务行锁后重新校验。

分发页默认显示待处理记录，可切换查看成功记录。审批未完成、旧稿件、账号缺失、发送未满十分钟、历史操作达到上限等情况提前显示原因。发送中显示最早可核对时间，时间到达后刷新并重新检查，绝不自动解除发送保护。API Key 不代表人工核对身份。

本次后端 779 项、前端 119 项测试通过，前端构建通过；本地浏览器模拟检查验证了审批阻止时隐藏表单和待办计数变化。九项 PostgreSQL 测试由推送后的 CI 临时数据库门禁强制执行，本轮本地普通测试入口按设计跳过它们。


## 客户单次审核与发布前检查清单（当前口径，覆盖前述独立审核要求）

按用户最新要求，GEO 仅需客户一次人工审核，客户可以审核自己提交的稿件。不再要求另一位审核人员，也不再使用 `geo_allow_self_review` 限制同账号确认。审批仍要求登录身份、当前客户操作权限；API Key 不能替代客户审核。提交待审是流程动作，不是另一道审批。AI 检查仅提供建议，发布的结构、事实、版本等技术检查保持不变。

任务编辑页新增可折叠的发布前检查清单，集中展示自动发布账号、渠道稿版本、客户审核、指标基线、当前母稿的发布核验和复测条件。入口复用渠道配置、分发页面及指标验收任务；支持直接建立关联验收任务。关联列表按客户与内容任务共同过滤，不混用其他文章的证据。列表最多展示 200 个关联任务并明确提示上限。

客户审核前须保存稿件并主动确认审核声明。新界面提交 `expected_article_id` 和 `expected_updated_at`；服务端在获取任务行锁后比对版本，变化时返回 409，要求刷新重审。已有旧客户端仍可使用原审批接口；新清单不替客户自动审批，也不自动发布或启动 AI 复测。

验证：782 项后端回归、123 项前端测试通过；9 项数据库专用测试待推送后的 CI 服务执行。内存模拟页面验证了单次客户确认，以及缺少账号、等待基线和未核实上线的展示。旧稿/任务修订冲突、未经勾选确认、未保存及切换客户的迟到响应均有测试。


## 诊断整改执行卡

验收工单列表展开行可查看整改执行卡。`GET /api/v1/geo/action-tickets/{ticket_id}/implementation-plan?tenant_id=...` 只读返回关联诊断的目标页面、诊断时间、原始问题依据、建议处理角色、执行步骤及验收口径。工单和诊断均按当前客户校验；不适用于统一指标任务或内容机会工单。优先使用工单保存的原始依据，避免后续诊断说明变动覆盖历史事实。

执行卡复用已有负责人、完成日期和状态接口，不引入新工单体系或第二道审批。实际页面改动仍需在客户网站实施；自动验收项应在上线后重抓验证，人工项须记录修改依据。整改通过不代表 AI 可见度提升，效果仍走指标验收。无效协议或携带用户名密码的地址不显示为页面链接。

同一诊断生成工单时，先获取诊断行锁，再查询已有建议代码并创建缺失工单，防止并发重复生成（保留原 replace_open 行为）。本轮测试覆盖锁定顺序和已有工单复用；跨诊断重复工作合并不在本次范围。列表及执行卡均丢弃客户切换后的旧响应。

验证：后端全量 789 项通过，9 项 PostgreSQL 专用测试在普通本地入口跳过；前端回归与构建通过。新功能没有执行真实网页修改或替客户验收。


## 跨诊断合并、持续发布监测与效果复盘（2026-09-06）

此节覆盖上节“跨诊断不在范围”及旧 replace_open 删除行为。相同客户、相同页面、相同问题的未完成工单在后续诊断中复用；保留原诊断、负责人、截止日期、原始依据和验收历史。关联诊断 ID 保存在 baseline_snapshot.diagnosis_ids，按新诊断筛选和批量验收也能找到原工单。地址仅规范化主机名、默认端口和片段，保留协议、路径大小写、尾斜杠及查询参数，避免合并不同页面。已完成问题在新诊断中再次出现时另建工单。同一诊断重复请求仍复用；replace_open 参数保留兼容，但不再删除工作历史。响应增加 merged 数量，items 包括新增及关联工单。生成采用客户行锁，再获取诊断及工单锁。

分发页增加“发布后监测”：
- GET /api/v1/geo/content-tasks/{task_id}/publication-monitor?tenant_id=...：只读发布监测状态，最近 100 条发布登记。
- POST /api/v1/geo/content-tasks/{task_id}/publication-monitor/{publication_id}/check?tenant_id=...：实际重抓检查，五分钟内重复点击复用结果。
- GEO 服务自有调度器每十分钟处理最多 25 条到期记录，最早到期优先。正常页面 24 小时后复查，异常 1 小时后重试；单次抓取总时限 25 秒，沿用 SSRF 防护、跳转与响应大小限制。无需任何 AI 请求，不自动重发文章。
- 新发布登记保存版本指纹，旧记录首次监测明确标记比较的是当前存储稿件，不能冒充历史发布基准。状态区分 pending、healthy、unreachable、mismatch、version_changed。healthy 表示正文匹配，不代表引用或提及改善；抓取失败也不能断言文章下线。
- 监测状态、最近 30 次结果保存在渠道稿 adapt_meta.publication_monitor，保留发送日志。通过内容行锁和渠道稿锁串行化检查与发布；崩溃回滚释放锁。连续两次异常创建唯一 monitor:v1 工单，恢复后实际重抓匹配才关闭，再次异常重新打开。不能手工打勾关闭，工单操作直接进入发布页检查。无数据库迁移。

效果复盘每天上海时间 09:15 评估最多 100 个未结束、关联内容的统一指标任务，最久未评估者优先。使用完整自然周、同题同引擎同模型同采样次数及已核实发布之后的观测；重新读取原始基线，来源被更正则等待重新核实。数据不足/口径变化记录 waiting，不能视为 0 变化。真实变化未达原目标时生成或更新唯一 review:v1 复盘工单，包含前后数值、周结束日期和样本证据；同一周不重复建单或重开已完成的复盘，新周仍未达标可重开。复盘完成须填写结论和下一步行动，不能代替原指标任务验收。观察达标也不自动完成原任务。execution-readiness 增加 outcome_review 说明，原 Task/Metric 最小字段格式不变。

用户页面：编辑器展示复盘状态，分发页展示发布监测，工单页处理异常及复盘，运营提醒汇总未完成跟进。客户审核仍只有一次；驾驶舱未接入。网站修改及真实渠道外发仍需要客户已有账号权限与客户确认。

验证：本轮后端 820 项通过，11 项 PostgreSQL 专项在普通入口跳过，并已在服务器独立临时空表环境全部通过；前端 131 项通过，构建通过。浏览器内存模拟验证发布异常恢复显示。推送时 CI 再以专用空数据库执行全量门禁。
