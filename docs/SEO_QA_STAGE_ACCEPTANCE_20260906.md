# SEO 问答分阶段验收记录（2026-09-06）

## 结论

真实平台发布仍未验收。本轮没有发布、修改生产配置或执行迁移。工作台扩展与 SEM 迁移不在本轮修复范围。

## 已定位并修复（本地待发布）

1. CSDN：生产只读查询确认 site_id=1 所属租户存在渠道 id=1，platform_code=csdn、mode=assisted、has_credentials=false、enabled=true、status=ready。它是辅助渠道配置，不是保险柜账号。界面误称“1 个账号”和“分发账号”，现改为渠道配置并明确登录未核验；不删除有效配置，不读取凭据。
2. 问答分发：同一站点真实 placement 数量为 0。审核通过不自动创建分发记录，核验需要先准备记录并回填公开地址。空列表新增站点批次入口，批次审核卡增加明确的准备分发文案，打开时选择该批次指定 answer_id。指定回答消失时明确报错，不切换其他回答；导航本身没有写入。

## 禁止自审

线上后端 RELEASE_COMMIT 为 5ef5bab99b5c1bbf67101bdd86546500bb0c1f5a，app/api/seo.py 第 7514 行存在实名独立审核拦截，超级管理员不能绕过。85 项问答后端测试通过，包含超级管理员自审、无用户 ID 审核拒绝及审核/分发流程。生产新审核行为未实操；不能把本地测试表述成生产人工验收。

生产最近记录仍存在提交人与审核人相同的历史数据，包括内容 id=15–19；不追溯修改。历史记录不能证明当前版本允许新自审，也不能追认成独立审核。

## 验证

- 问答后端：85 passed。
- 前端：54 passed；新增批次回答精确定位、缺失时不换稿、导航不创建记录回归。
- SEO 构建成功，产物校验 52 files；git diff --check 通过。
- 人工代码复查完成；无数据库字段或接口变更。
- 尚待修复版发布后的页面复核；真实平台发布单独验收。

## 提交后完整复查

基于修复提交 c3279162a5296892bbd0d2bdfcca90d0888dd33b，复查本次入口修复及前一批独立审核、批次控制变更；未发现新增阻断问题，未扩大业务功能。

- SEO 后端回归：928 passed，1 条第三方 pkg_resources 弃用警告。执行 tests/test_seo_*.py（排除 migration_merge），使用本地隔离测试库及浏览器模拟环境；不执行生产迁移。
- 全部 frontend/scripts/test-seo-*.mjs：124 passed，无失败或跳过。
- SEO 构建通过，52 个产物校验通过。保留构建工具的依赖注释与包体积提示，不将其当成功能失败。
- 本地 Chromium 实际点击：分发空列表→站点批次→已审核回答→准备分发对话框。导航没有产生分发写入；当前无公开网址时核验按钮保持禁用。
- 本地 Chromium 审核流程：逐条提交、通过、退回、必填退回意见、失效证据拦截；桌面与移动视口运行，无页面脚本错误。已查看移动截图。
- 差异格式检查通过。生产账号、真实发帖、生产新审核操作以及部署后的入口页面仍不计入本轮验收；本轮不推送、部署或迁移。

详细日志与浏览器测试脚本保存在本机 seo-local-validation-20260905/ 下的 qa-final-review-*、verify_qa_entry_review_ui.py；这些是本地验证产物。
