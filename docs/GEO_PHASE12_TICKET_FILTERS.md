# GEO 待办筛选与编辑保护

执行待办新增状态、负责人、期限筛选及关键词搜索。期限包括已逾期、今天到期、未设置；可重置筛选。展示当前筛选数量和总数量，顶部未完成/逾期统计保持当前客户全量口径。排序先未完成、再逾期、再截止日期，无期限在后。

修复原先保存负责人或状态后重新读取全列表导致验收草稿被清空的问题：保存成功只合并返回的那条记录，保留其他待办的编辑内容；手动刷新按字段保留本地已修改的负责人/日期与核验结果，未修改字段使用新服务端值。提交失败保留输入；成功提交核验结果仅清理本次已提交且未再修改的文字。

草稿仅在当前组件内存保留，不是自动保存。离开页面、刷新整个浏览器或切换客户会清空未提交草稿；切客户同时重置筛选，过期保存响应不会恢复旧客户数据。

验证：前端 27 项检查通过，GEO 构建通过，日志 geo-ticket-filters-node.txt / geo-ticket-filters-build.txt。本轮后端未改动，514 项后端通过为上轮结果。本地实际 Vue 页面与内存接口验证保存负责人/刷新后核验草稿仍保留、逾期筛选空结果提示、重置筛选后继续编辑、按负责人筛选、切客户重置均通过；控制台无 error/warn。构建保留既有大包提示。

仅修改 GEO 前端、测试与记录。未提交、未推送、未部署；前一轮 0074 数据库迁移仍未在实际数据库执行，真实数据库/接口验收限制不变。
