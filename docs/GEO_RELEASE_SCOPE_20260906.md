# GEO 本轮发布范围

用户已授权提交、部署。代码基准 d100907；准确部署 SHA 和上线只读验收结果在发布后补充。

## 内容与证据包

- Brief/策略保留内部元数据，不追加进正文免责声明；版本栏展示保存版本、文章 ID 与来源。
- 案例检查区分资料请求与真实断言，完整句子报错；保留混合句、主体、条件及否定的保护。
- 跨语言原文仅作为待核验候选，不自动认证翻译，不新增第二道客户审核。
- 不重新生成、审核、发布或清理任务14；H1仍待真人复验。

## 工作台只读包

- 9个查询入口与前端调用适配，使用独立 READ ONLY / REPEATABLE READ 事务，不初始化配置或回写超时。
- 本轮新增：新只读入口和正式指标/字典逐请求检查 GEO 开通/到期，复用 tenant_modules 的 active/trial、到期日含当天与现有 date.today()；用户权限/客户归属仍走 require_scoped_auth。不改变共享认证或日期策略。
- 查询失败不放行，管理 Key 也不能绕过这些入口的客户开通条件。
- 其余旧业务入口统一资格覆盖、AUTH-01/ID-01 和工作台最终页面接线仍属后续工作；不宣称所有旧 GEO 路由已补齐。
- 新增隔离 PostgreSQL 测试，在专用 CI 数据库的随机 schema 验证实际查询及数据库强制禁止写入，结束删除测试 schema；不是生产迁移。

边界：只部署 GEO；不修改 cockpit-foundation、SEM、SEO 或数据库结构，不启动真实采集、生成或再次检查。

已有设计/测试留档保留当时结论；本文件中的逐请求资格校验是对此前“尚未补齐”的后续修复，线上版本以发布后验证为准。
