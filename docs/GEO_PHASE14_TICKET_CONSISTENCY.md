# GEO 待办一致性检查

本轮修复：

- 新工作建议待办的 PATCH 与创建采用相同的客户行锁顺序；获得客户锁后，对工单加行锁并强制刷新 ORM 实例，避免依据等待锁之前读到的旧记录重写 evidence。
- 已完成旧单重新打开时，若同客户同建议代码已有未完成工单，返回 409 并给出已有工单编号；不会修改旧单。通过“验收未通过”重新打开也执行相同检查。
- 新工作建议工单仅通过带结果说明的人工验收写入验收结果。单条自动验收拒绝该类工单；诊断批量验收对其返回 skipped 和人工处理说明，保留原有记录。原有工单页隐藏该类工单的自动验收按钮，仍保留人工通过/驳回。

范围和限制：只对 workqueue:v1 前缀工单实施新增锁和冲突检查，旧工单修改流程不使用该客户锁。锁以客户为粒度，可能使同客户不同待办的写入排队；本次没有实施基于版本号的旧页面编辑冲突提示，也没有清理历史重复记录。近期记录仍只保留最近 6 条。本轮没有新增数据库迁移；先前 0074 迁移仍未执行到实际数据库。

验证：后端 523 项、前端 28 项检查、GEO 构建和 git diff --check 通过。新测试覆盖锁后重新读取最新记录、重复重新打开拦截、验收失败重新打开拦截、自动验收不改记录、批量跳过及旧工单兼容。
日志：geo-ticket-consistency-tests.txt、geo-ticket-consistency-node.txt、geo-ticket-consistency-build.txt。构建保留既有大包提示。

后端测试采用数据库替身，不能代替 PostgreSQL 实库并发/持久化验证；本轮没有运行浏览器模拟。未调用真实模型或生产服务，没有提交、推送、部署。
