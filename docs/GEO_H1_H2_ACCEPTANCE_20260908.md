# GEO H1 通过与 H2 阶段验收记录（2026-09-08）

## 版本与边界

- 生产 SHA：`63fefec53906c99d78c3d800395af44afd72d1e1`
- 客户：诺德
- 内容任务：`#14`
- 本文只记录真人回传结果和后续操作要求。开发人员未代替真人登录、编辑、审核或发布。
- 本轮不配置真实发布账号，不调用真实发布平台，不执行 H3/H4。

## H1 最终复验：通过

真人在现有正文中仅补充三条由已绑定事实支持的可抽取步骤，保存一次；随后分别执行一次
“刷新发布检查”和一次“按当前品牌重新检查”。本次没有重新生成母稿、审核、生成渠道稿或发布。

| 项目 | 结果 |
| --- | --- |
| 最新母稿 | V5，基于 V4，文章 `#22`，约 1073 字 |
| 历史版本 | V1/V2/V3 均保留 |
| GEO 分数 | 94 |
| 发布检查 | 评分侧显示“可以发布” |
| 原阻塞项 | “可抽取步骤 / 缺少操作步骤块”不再出现 |
| 事实边界 | 新增步骤仅使用既有三条事实，没有重新生成或加入新事实 |

页面仍显示的六项属于后续流程条件，不回退 H1 结论：发布账号未配置、渠道稿版本待一致、
待客户审核、指标基线未关联、上线未核实、复测验收未建立。

## H2 Phase A：通过

- 官网、微信、知乎三个测试渠道稿均已生成，状态均为 `draft`。
- 审核前“推送发布”不可用。
- 页面提示当前不能自动推送/未配置发布账号。
- 普通客户审核账号 `nord_geo_review` 已登录并尝试审核；任务当时仍为“待提交客户审核”，
  页面提示“当前角色无权编辑此功能”，没有产生审核结果。
- 真人已退出审核账号，并在超管登录页填写凭据；当前停在验证码步骤。

Phase A 只证明审核前发布门禁和渠道稿准备状态，不代表 H2 整体通过。

## H2 当前受阻结论

本次尝试发生在编辑侧提交客户审核之前。GEO 审核状态机要求母稿先从未提交状态进入 `pending`，
客户审核账号才可以执行通过或拒绝。因此这次提示不能单独证明审核账号权限配置错误，也不能算 H2 失败；
当前结论为“前置提交未完成，受阻”。

如果编辑侧提交后页面已显示“待客户审核”，`nord_geo_review` 再次执行审核仍提示
“当前角色无权编辑此功能”，则停止验收并按审核角色权限缺陷记录，不扩大该账号权限。

## H2 后续真人操作

验证码由真人完成，开发人员不得读取、转录或代输。按以下顺序操作：

1. 真人完成超管或获授权编辑账号的验证码，确认当前客户为“诺德”；身份或客户不符立即停止。
2. 打开任务 `#14`，确认当前母稿仍为 V5/文章 `#22`，三个渠道稿仍为既有 draft；
   不编辑、不重新生成、不发布。
3. 点击“提交客户审核”一次并等待完成。记录提交前后状态、时间；预期状态变为“待客户审核”或
   `pending`。若提交失败，记录接口状态和提示后停止。
4. 退出编辑账号，由真人登录 `nord_geo_review` 并完成验证码；确认当前客户仍为“诺德”。
5. 打开任务 `#14`，记录 V5/文章 `#22`、三个 draft 渠道稿各自的 ID、关联母稿 ID 和审核前状态。
6. 仅对当前 V5 执行一次客户审核通过；不编辑正文、不生成渠道稿、不点击发布。
7. 如果此时仍提示“当前角色无权编辑此功能”，记录 `/api/v1/geo/content-tasks/14/review`
   的 HTTP 状态、响应错误文字和页面审核状态（不得记录请求头或令牌），然后停止。
8. 审核成功后刷新页面，记录审核状态、审核人、审核时间，以及三个渠道稿是否仍关联文章 `#22`。
9. 退出审核账号，切换到获授权的内容编辑账号。
10. 在 V5 做一次不增加新事实的小改并保存新版本。建议只把步骤块标题从“选型核对步骤”改为
    “MAXXDRIVE 选型核对步骤”；不要改事实、数值、品牌画像、任务标题或 CTA。
11. 刷新页面，记录新母稿版本和文章 ID，确认 V5 的批准不能授权新版本。
12. 检查原三个渠道稿：必须显示版本不一致、已过期或要求重新生成；不得用于发布。
13. 记录发布按钮禁用状态和完整提示。未配置账号不是版本失效的唯一证据，必须同时保留渠道稿所关联
    的旧文章 ID 和页面版本提示。
14. 重新生成官网、微信、知乎渠道稿各一次，确认新稿均关联最新母稿；记录新渠道稿 ID。
15. 到此停止。不配置发布账号，不重新审核，不执行发布，不进入 H3/H4。

## H2 通过标准

- `nord_geo_review` 能作为诺德普通客户完成一次审核，无需第二名审核人员。
- 未审核版本不能发布。
- 保存新母稿后旧批准立即失效，旧渠道稿不能授权或发布新版本。
- 重新生成的三个渠道稿都关联最新母稿。
- V1 至 V5 及本次新版本、历史审核信息均保留。

## 禁止动作

- 不读取、记录或回传验证码、密码、Cookie、Token 或渠道密钥。
- 不修改客户名、优化业务名、`profile.product_name`、事实卡、任务标题或历史版本。
- 不配置真实渠道账号，不点击任何真实发布确认，不调用真实发布平台。
- 不连续点击审核、保存或渠道稿生成；每个指定动作只执行一次并等待明确结果。
- 不删除任务 `#14`、历史母稿或渠道稿。

## 回传字段

```text
用例编号：H2-客户一审与版本失效
生产 SHA：
日期与时区：Asia/Shanghai
浏览器/操作系统：
登录用户名、页面显示身份、客户：
任务 ID：
提交客户审核前状态：
提交客户审核结果、提交时间：
审核前母稿版本/文章 ID：
审核前官网/微信/知乎渠道稿 ID及关联文章 ID：
审核结果、审核人、审核时间：
小改内容：
保存后母稿版本/文章 ID：
旧批准失效结果：
旧渠道稿版本提示原文：
发布按钮状态与禁用原因原文：
新官网/微信/知乎渠道稿 ID及关联文章 ID：
历史版本是否保留：
控制台或网络错误（不含令牌）：
结论：通过 / 失败 / 受阻
截图时间：
清理结果：不清理，保留给后续验收
```
