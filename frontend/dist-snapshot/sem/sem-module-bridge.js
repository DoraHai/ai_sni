// Retired: module navigation now lives in the native left sidebar in every app.
// Keep this cleanup for cached SEM documents that still request the old script.
document.getElementById('sem-module-bridge')?.remove()
