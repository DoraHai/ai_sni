server {
    listen 80;
    server_name gsnipers.snipers.com.cn;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 200 "G-Snipers domain setup in progress\n";
        add_header Content-Type text/plain;
    }
}
